# 나라장터 법령위반 모니터링 AI 경진대회 — 개발 계획

작성일 2026-09-05 · 근거: rule/score/summary/etc1·2.txt, open.zip 실측 분석

---

## 0. 한 줄 전략

**"메타데이터 기반 결정론적 게이팅 + 항목 그룹별 집중 LLM 추출 + 규칙 결합"**
— 24항목 한 방 프롬프트(베이스라인)를 버리고, 각 항목을 `적용조건(규칙)` × `사실추출(LLM)` 로 분해한다.

---

## 1. 채점 구조가 강제하는 것

| 사실 | 함의 |
|---|---|
| Macro F1 = 24개 항목 **양성(=1) 클래스 F1의 단순평균** | 항목 하나를 전부 0으로 내면 그 항목 F1=0 → **24개 중 1개도 포기 불가**. 항목별 기여도 동일(각 4.17%) |
| dev 200건 중 항목당 양성 5~8건 (실측) | 양성 희소. 정밀도/재현율 균형점이 점수를 지배 |
| 평가 데이터 1,853건, 24항목 모두 양성 존재 보장 | 전부 0 예측 항목은 F1 0 확정 |
| 1일 1회 제출 | **로컬 검증 루프가 곧 경쟁력.** 제출은 로컬 지표 개선 시에만 |
| 근거문구(e열) = 2차 정성평가 20% | 리더보드엔 무영향이나 최종 수상 결정에 영향. 원문 부분문자열 강제 |

### 시간 예산 (가장 빡빡한 제약)

```
총 2시간(7,200s) = 모델 로드(26B int8, 추정 300~600s) + 추론
→ 1,853건 기준 건당 약 3.4~3.7초 (연속 배칭 기준 처리량, 지연시간 아님)
```
gemma-4-26B-A4B = MoE(활성 4B)라 디코딩은 빠르나 **프리필이 병목**.
프롬프트 8k 토큰 × 1,853건 = 14.8M 프리필 토큰.
→ **패스 수 2~3회가 상한.** Phase 0에서 실측 없이 설계 확정 금지.

---

## 2. 데이터 실측 분석 (직접 확인한 것)

### 2-1. 문서 길이 — 베이스라인의 치명적 결함

```
공고당 총 글자수  min 2,868 / median 13,218 / p90 39,459 / max 54,068
베이스라인 build_context(max_chars=4000)
```
**중앙값 문서의 70%, p90 문서의 90%가 프롬프트에 안 들어간다.**
입찰참가자격 조항은 공고문 중·후반부에 있어 통째로 잘림 → 베이스라인 점수 상한이 여기서 막힘.
→ **섹션 추출/후보 스팬 검색이 1순위 개선 포인트.**

### 2-2. 메타 기반 게이팅 — dev 200건에서 100% 일치 (검증 완료)

`입찰추정가격` 밴드: A=`<1억`, B=`1억~2.3억(고시금액)`, C=`≥2.3억`

| 항목 | dev 양성의 조건 분포 | 게이팅 |
|---|---|---|
| v5 (고시금액 이상 지역제한) | C 7/7 | **C 아니면 0** |
| v14 (고시금액 이상 중소기업 제한) | C 8/8 | **C 아니면 0** |
| v15 (1억~고시 소기업 제한) | B 6/6 | **B 아니면 0** |
| v16 (1억~고시 중소 제한없음·부재) | B 6/6 | **B 아니면 0** |
| v17 (1억 미만 중소기업 제한) | A 6/6 | **A 아니면 0** |
| v18 (1억 미만 소기업 제한없음·부재) | A 7/7 | **A 아니면 0** |
| v9 (모델명 명시) | 물품(내자) 6/6 | 용역이면 0에 가깝게 |
| v22 (현장설명회 참석제한) | 협상에의한계약 5/5 | **협상 아니면 0** |
| v23 (현장설명회 공고기간) | 협상+지방계약법 5/5 | **협상+지방 아니면 0** (항목표 비고와 일치) |

부분 게이팅(예외 존재 — 지방 소액수의 특례로 추정):
v2(A 6, B 1) · v6(A 4, B 1, C 1) · v7(B 4, A 2, C 1) · v3(C 4, A 3, B 1)

> ⚠️ 고시금액 2.3억은 **국가계약법 물품·용역** 기준. 지방계약법은 시행규칙 제24조 별도 기준,
> 공사는 88억. 밴드 계산기는 `적용계약법 × 업무구분`으로 분기해야 함.
> 또 실적제한 배수 기준이 국가 **1배 이내**(시행규칙 25②) vs 지방 **규모·양 1/3 이내, 금액 1배 이내**(시행규칙 25②)로 다름 → v3 로직 분기 필수.

### 2-3. 중기간 경쟁제품 (v10~v13) — 결정론적 룩업 **실패**, 재설계 필요

```
중기부고시 세부품명 616개 (CSV 파싱 OK)
dev 200건 중 meta.세부품명번호목록 등록 78건, 그중 경쟁제품 매칭 17건
그러나 v10~v13 양성 25건 중 meta 경쟁제품 매칭 = 0건
본문 10자리 품명번호 등장 기준으로는 v13 4/6, v11 3/6, v12 3/6, v10 1/7 부분 일치
```
→ meta 필드만으론 판정 불가. **본문의 품명·규격 텍스트 ↔ 고시 세부품명 616개 이름 매칭**(임베딩 bge-m3 또는 형태소 기반)이 필요. 연구 항목으로 분리.

### 2-4. 근거문구

dev 라벨의 근거문구 54개 **전부 원문 부분문자열**(검증됨). 개행(`\n`) 포함 인용 존재.
베이스라인은 부분문자열 아니면 그냥 버림 → **근접 스팬 복원(fuzzy → exact anchor) 로직으로 회수율 개선 여지.**

### 2-5. 기타 메타 관찰

- `정보화사업여부` dev 200건 전부 `"미입력"` → **v20(SW) 판정에 메타 사용 불가, 본문에서 SW사업 판별 필요**
- `조달방식` 전부 `자체조달`
- 계약방법: 제한경쟁 135 / 수의계약 35 / 일반경쟁 30
- 적용계약법: 지방 116 / 국가 84

---

## 3. 아키텍처

```
공고 1건
  │
  ├─[1] 전처리 · 섹션 파서 (규칙, CPU)
  │      · 입찰참가자격 / 제한사항 / 현장설명회 / 공동수급 / 낙찰자결정 / 제출서류 섹션 분리
  │      · 금액·기간·지역·업종·품명번호·모델명 후보 정규식 추출
  │      · 문서 전체 → 항목그룹별 관련 스팬으로 압축 (bge-m3 + BM25 하이브리드)
  │
  ├─[2] 적용조건 게이팅 (규칙, CPU)  ← §2-2
  │      메타 + 법령 임계값 테이블 → 각 항목 {적용, 미적용(=0 확정)}
  │
  ├─[3] LLM 사실추출 (고정 LLM, 그룹별 프롬프트)  ← 규칙상 공고당 1회 이상 호출 필수
  │      G1 참가자격 제한  : v1 v2 v3 v4 v5 v6 v7 v8
  │      G2 기업규모·직생  : v10 v11 v12 v13 v14 v15 v16 v17 v18
  │      G3 물품·SW·공동  : v9 v19 v20 v21
  │      G4 현장설명회·대조: v22 v23 v24
  │      · 구조화 출력(JSON Schema) + temperature 0
  │      · 게이팅에서 미적용된 항목은 프롬프트에서 아예 제외 → 토큰 절감·정확도 상승
  │
  ├─[4] 규칙 결합 + 임계 보정 (CPU)
  │      LLM 추출사실 × 법령 임계값 → 최종 0/1
  │      항목별 의사결정 임계 = dev + 자가라벨 검증셋에서 F1 최대화로 튜닝
  │
  └─[5] 근거문구 정합 + CSV 검증
         근접 스팬 복원 → 원문 부분문자열 보장 → 49열 자가검증
```

**설계 근거**: 항목별로 필요한 컨텍스트가 완전히 다르다. 24개를 한 프롬프트에 넣으면
(a) 4,000자 잘림, (b) 활성 4B 모델의 주의력 분산, (c) 항목별 임계 조정 불가.
그룹 분할은 정확도와 튜너빌리티를 동시에 얻는 대신 패스 수를 늘리므로 §1 시간예산 내에서만 허용.

---

## 4. 단계별 실행 계획

### Phase 0 — 시간예산 실측 (최우선, 다른 모든 설계의 전제)
- [ ] GPU 환경 확보. **로컬 M5/16GB로는 26B 모델 불가** → 클라우드 L40S/A100 대여 필요
      (대안: MLX 4bit로 프롬프트 형태만 로컬 검증 — 점수 재현성은 없음)
- [ ] 베이스라인 `script.py` 그대로 1,853건 상당 부하 실행 → 실제 tok/s, 로드시간 측정
- [ ] **산출물: "패스 N회 × 프롬프트 M토큰"의 실행 가능 영역 표.** 여기서 그룹 수 확정
- [ ] 첫 제출 = 베이스라인 무수정 (리더보드 기준선 확보, 채점 파이프라인 검증)

### Phase 1 — 오프라인 평가 하네스
- [ ] dev 200건 Macro F1 계산기 (제출 산식과 동일하게 재현)
- [ ] 항목별 F1 / precision / recall / 혼동행렬 리포트
- [ ] 근거문구 부분문자열 적중률 지표
- [ ] 실행 시간 프로파일러 (건당 프리필/디코드 토큰)
- **게이트: 이게 없으면 어떤 개선도 측정 불가. Phase 2 이전에 반드시 완료**

### Phase 2 — 규칙 레이어 (LLM 없이 얻는 점수)
- [ ] 법령 임계값 테이블 구축 (하드코딩 아님 — 배포 법령패키지에서 파싱, 출처 주석 필수)
      · 고시금액: 국가 물품·용역 2.3억 / 공사 88억 / 지방 별도
      · 실적제한 배수: 국가 1배 · 지방 규모양 1/3, 금액 1배
      · 중복제한 금지 예외(국가 시행규칙 25⑤: 6호+2호, 8호·10호는 중복 허용) → **v8 로직의 핵심**
      · SW 대기업참여제한 하한 (지침 별표1: 20/40/80억 구간)
      · 공동수급 최소지분율 (공동계약운용요령)
- [ ] 메타 게이팅 구현 → dev에서 **거짓양성 감소량** 측정
- [ ] 섹션 파서 + 정규식 후보추출기
- [ ] v24 대조 로직: 본문 추출값(예산·계약방법·지역제한·업종) vs meta 규칙 비교

### Phase 3 — LLM 파이프라인
- [ ] 그룹별 프롬프트 + JSON Schema 설계 (게이팅 결과로 동적 축소)
- [ ] RAG: 조문 검색을 **질의=공고문 앞부분(베이스라인)에서 항목별 고정 조문 주입으로 교체**
      — 항목표.json에 항목→조문 매핑이 이미 고정으로 주어져 있음. BM25 검색보다 정확
- [ ] bge-m3 활용 판단: GPU 동시적재 불확실 → **CPU 임베딩 or 사전계산 인덱스(model/)** 로 설계
- [ ] 근거문구 근접 복원 로직

### Phase 4 — 자가 라벨링 (검증셋 확장)
- 목적: dev 200건은 "평가 분포를 대표하지 않음"(README 명시) → 과적합 위험
- [ ] 20,000 무라벨에서 층화 샘플링 (적용계약법 × 업무구분 × 금액밴드 × 계약방법)
- [ ] 외부 LLM으로 라벨 생성 (**규칙상 라벨 생성 단계만 허용**, 입력은 대회 제공 자료만)
- [ ] 생성 코드·프롬프트·모델버전 기록 보관 → **2차 평가 재현 대상**
- [ ] dev 200 + 자가라벨 N건으로 항목별 임계 튜닝

### Phase 5 — 튜닝 · 제출 운영
- [ ] 항목별 결정 임계 최적화 (Macro F1 목적함수, 양성 희소 고려)
- [ ] 실패 케이스 분석 루프
- [ ] 제출 체크리스트 자동화: 49열 · 행수 · id 유일 · v∈{0,1} · e 500자 · 부재탐지 5항목 공란 · UTF-8 no BOM · NFC
- [ ] 제출 로그(무엇을 바꿨고 점수가 어떻게 변했는지) — 1일 1회 제약상 필수

---

## 5. 규칙 준수 체크리스트 (실격 방지)

- [x] 파인튜닝·어댑터 금지 → 규칙 레이어와 프롬프트만 사용
- [x] 학습된 가중치로 위반판정 금지 (분류기·회귀 포함) → **임계값 튜닝은 "규칙 상수 조정"으로 한정, sklearn 모델 학습·직렬화 금지**
- [x] 공고당 고정 LLM 정상호출 1회 이상 → 게이팅으로 전 항목 0이 되는 공고도 **LLM 호출은 반드시 수행**
- [x] 추론 코드 내 외부 API 호출 금지
- [x] 공고 단위 독립 예측 → 다른 샘플의 통계·예측값 사용 금지 (전역 정규화·배치 통계 금지)
- [x] 절대경로 하드코딩 금지 → `PPS_*` 환경변수만
- [x] `if __name__ == "__main__":` 아래에 vLLM 실행
- [x] test.jsonl.gz의 id·행수 하드코딩 금지
- [x] model/ 에는 정적 자산만 (RAG 인덱스 OK, 가중치 불가)

---

## 6. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| **2시간 초과 → 제출 오류(횟수 차감)** | 치명 | Phase 0 실측 선행. 워치독 타이머 + 남은 건 기본값 기록 후 조기 종료 |
| dev 200 과적합 | 중대 | Phase 4 자가라벨 검증셋. 밴드 게이팅 같은 **법령 근거 있는 규칙만** 신뢰, 순수 상관은 배제 |
| GPU 환경 미확보 | 치명 | Phase 0 최우선. 클라우드 비용 사전 확보 |
| v10~v13 경쟁제품 판정 (§2-3) | 항목 4개 = 16.7% | 별도 연구 트랙. 본문 품명 텍스트 매칭 |
| 지방/국가 임계값 혼동 | 다수 항목 | 임계값 테이블에 조문 출처 주석 의무화 |
| 1일 1회 제출 | 반복 속도 | 오프라인 하네스가 실질적 리더보드 역할 |

---

## 7. 지금 당장의 다음 3가지

1. **Phase 0 GPU 환경 결정** — 가장 큰 미결 변수. 이게 안 정해지면 Phase 3 이후 전부 정지
2. **Phase 1 평가 하네스** — GPU 없이 지금 바로 만들 수 있음
3. **Phase 2 법령 임계값 테이블** — GPU 없이 지금 바로 만들 수 있음. 점수의 상당 부분이 여기서 나옴
