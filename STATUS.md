# 진행 상황 — 2026-09-07

## ⛔ 폐기된 결론: "예측량이 병목이다"

09-06 에 세운 이 가설로 하루를 썼고, **09-07 제출이 0.42458 → 0.31943 으로 떨어뜨려
반증됐다.** 아래에 증거를 남긴다. 다시 이 방향으로 가지 말 것.

## ✅ 실측된 결론: 우리는 **재현율**이 병목이다

제출 2회로 얻은 통제된 비교다. 두 제출의 차이는 **전부 억제 방향**이었다
(게이트 5개 강화 · 프롬프트 배제조건 추가 · 중소기업 정규식 확대로 v11 억제 강화).

| 제출 | 커밋 | 게이팅 차단율(실제분포) | 오프라인 상한 | dev F1 | **리더보드** |
|---|---|---|---|---|---|
| 09-06 | `24ccf2d` | 31.2% | 0.4883 | 0.6326 | **0.42458** (205/748) |
| 09-07 | `3471ccf` | **40.5%** | 0.5041 ↑ | 0.6455 ↑ | **0.31943** ↓ |

**예측 칸을 9.3%p 줄였더니 점수가 0.105 떨어졌다.**
dev 도 상한도 "좋아졌다"고 말했는데 실제는 크게 나빠졌다.
→ 잘라낸 칸에 거짓양성보다 **진짜 양성이 더 많았다**는 뜻이다.

### 오프라인 지표 두 개가 모두 실패했다 — 왜인지가 중요하다

**`calibrate.py` 의 상한은 억제를 평가하는 데 쓸 수 없다.**
상한은 `정밀도 ≤ 실제양성률/예측률` 로 계산하고 **재현율은 dev 값을 그대로 쓴다.**
게이팅은 정의상 예측률을 낮추므로 상한이 **기계적으로 올라간다.**
그러면서 게이팅이 죽인 진짜 양성은 dev 에서 안 보이면 반영되지 않는다.
→ **억제 변경을 상한으로 평가하면 항상 좋아 보인다. 구조적으로 눈이 먼 지표다.**

**dev 도 이 변경을 걸러내지 못했다** (+0.013 이라고 말했다).
"게이팅이 죽인 진짜 양성 0건"의 근거였던 dev 양성은 19건뿐이다.
0/19 의 95% 신뢰상한은 15.8%(rule of three) — **0건 관측은 안전의 증거가 아니다.**

### 게이트가 실제 분포에서 더 막은 것도 아니다 (측정함)

| 게이트 통과율 | dev200 | holdout200(실제분포) |
|---|---|---|
| 세부품명 미등록 (v10·v11·v13) | 61.0% | 62.5% |
| 업무구분 물품 (v9) | 39.0% | 37.5% |
| 직생요구 문구 존재 (v12) | 66.5% | 53.5% |

분포 차이는 거의 없다. 즉 **게이트 자체가 진짜 양성을 자른 것**이지,
"dev 에만 맞는 게이트였다"는 설명이 아니다. 조문에서 유도했다는 근거도
실제로는 조문이 아니라 **dev 상관관계**였다 — 세부품명 등록 여부는
어느 조문에도 나오지 않는다.

### 놓침의 원인은 하나로 모인다 (dev200g 분석)

`tools/analyze_errors.py` — 놓침 51건의 원인 분류:
```
모델오판 50건 · 입력누락 1건 · 게이팅차단 0건
```
배관(게이팅·섹션·규칙)은 멀쩡하다. **근거가 프롬프트에 들어갔는데 모델이 0 이라 답한다.**

그리고 놓침 51건 중 **42건(82%)이 "그룹 전체 침묵"** — 같은 그룹의 다른 항목도
전부 0 이었다. 형제 항목 혼동(9건, 18%)이 아니다.
→ 항목 정의를 다듬는 문제가 아니라 **판정 문턱 문제**다. 전 항목에 걸린다.

**v20 은 약체가 아니다.** dev200h 의 v20 재현율 0.20 은 API 장애로 생긴
빈 출력 58건 탓이다. 깨끗한 dev200g 에서는 0.80 이다. 최약체 목록에서 빼라.

---

## dev 는 평가 분포를 대표하지 않는다 (실측)

`train_unlabeled` 20,000 건과 견주면 dev 는 **큰 계약에 편중**돼 있다.

| 판로지원 밴드 | dev | 실제 20,000건 |
|---|---|---|
| A (1억 미만) | 53.0% | **71.7%** |
| B (1억~고시금액) | 24.5% | 17.1% |
| C (고시금액 이상) | 22.5% | **11.2%** |

그 결과 **노력 배분이 틀렸었다**. 게이팅 통과율 = 그 항목이 적용되는 공고 비율:

| 항목 | dev | 실제 | 우리 F1 | |
|---|---|---|---|---|
| v17 | 53.0% | **71.7%** | 0.400 | 실제로 훨씬 중요 |
| v18 | 53.0% | **71.7%** | 0.333 | 실제로 훨씬 중요 |
| v23 | 20.5% | **5.8%** | 0.909 | dev 에서 3.5배 부풀려짐 |
| v22 | 42.5% | 24.3% | 0.889 | |
| v8 | 85.5% | 60.3% | 0.706 | |

**v17·v18 은 실제 공고의 72%에 적용되는데 우리 최약체다.** 정밀도 0.24~0.50 이라
실제 분포에서 거짓양성을 대량 생산한다.

---

## 데이터 역할 분리 (이번에 확립)

dev 하나로 튜닝과 검증을 겸하면 안 된다.

| 데이터 | 역할 | 라벨 |
|---|---|---|
| `dev.jsonl.gz` 200건 | 판정 **정확성** 검증 | 있음 |
| `train_unlabeled` 20,000건 | 예측률 **분포 보정** | 불필요 |
| `out/holdout200.jsonl.gz` | 실제분포 층화표본 홀드아웃 | 없음(예측률만) |

**예측률은 정답 없이도 잰다** — 다만 그것으로 상한을 계산하려면 실제 양성률을 알아야 하는데
**우리는 모른다.** `--true-rate 0.02` 는 근거 없는 가정이었고, 그 위에 세운 상한이
09-07 제출을 잘못 승인했다. 예측률은 **관찰값으로만** 쓰고 의사결정에는 쓰지 않는다.

### dev 는 인위적으로 균형 잡힌 표본이다 (이번에 확인)
항목별 양성이 **전부 5~8건**이다. 24개 서로 다른 위반유형이 우연히 이렇게 고를 수 없다.
```
v1 7 · v2 7 · v3 8 · v4 6 · v5 7 · v6 6 · v7 7 · v8 6 · v9 6 · v10 7 · v11 6 · v12 6
v13 6 · v14 8 · v15 6 · v16 6 · v17 6 · v18 7 · v19 6 · v20 5 · v21 6 · v22 5 · v23 5 · v24 8
평균 양성률 3.19%   (STATUS 가정치였던 2.00% 가 아니다)
```
평가셋에서 희귀한 항목은 양성이 훨씬 적어 F1 이 무너진다. **dev 는 수준을 못 맞춘다**
(dev 0.63 ↔ 실제 0.42). 밴드 편중까지 겹친다: dev A 53.0% vs holdout200 A 71.5%.

`out/holdout200.jsonl.gz` 는 20,000 건에서 밴드·계약법·업무구분 비율을 유지해 뽑은 200건이다
(모집단과 오차 1%p 이내).

---

## ⛔ 반증된 경로: 조문·정의 기반 게이팅 (2026-09-07 제출로 폐기)

경쟁제품 4형제(v10·v11·v12·v13)와 v9 를 게이팅했다.
dev 는 0.6326 → 0.6455, 상한은 0.4883 → 0.5041 로 올랐다.
**리더보드는 0.42458 → 0.31943 이었다.** 되돌렸다.

### 무엇이 틀렸나
`meta.세부품명번호목록` 미등록 게이트의 근거는 "dev 양성 19건이 전부 미등록"이었다.
이건 **조문이 아니라 dev 상관관계다.** 세부품명 등록 여부는 판로지원법 어디에도 없다.
등록률이 dev 39.0% ≈ train_unlabeled 37.6% 라는 '분포 검증'은
**게이트가 진짜 양성을 자르는지와 무관한 검사였다** — 통과해도 아무것도 보증하지 않는다.

v9 의 `is_물품` 게이트는 이미 알고 있던 실수를 반복한 것이다
(아래 실패표: "항목명의 '일반물품'은 업무구분이 아니다").

**게이팅 채택 기준을 다시 쓴다** — 아래를 전부 만족할 때만 켠다.
1. 조문이 **적용 대상 자체를 한정**할 것 (예: 금액 밴드, 계약법). 메타 필드의 존재 여부는 근거가 아니다.
2. 게이트가 없으면 그 항목이 **논리적으로 성립 불가**할 것.
3. dev 에서 "죽인 양성 0건"은 **필요조건일 뿐 충분조건이 아니다** (양성 6~8건 → 15% 오차).

---

## 09-08 제출 후보 = 09-06 기준선 + 검증된 버그 수정 + v21 사실제공

커밋 안 된 작업트리 상태다. 억제는 **하나도** 넣지 않았다.

**되돌린 것** (전부 억제 방향)
- 게이트 5개(v9·v10·v11·v12·v13) → 전부 `_always`.
  **게이트 함수가 기준선과 완전히 일치함을 diff 로 확인했다**
- `_rule_hints` 의 v10·v11 강제 0 — dev 에서 v10 양성 7건 중 4건, v11 6건 중 2건을 죽이고 있었다.
  '직접생산'이 계약조건 상투문구로 200건 중 133건에 등장하는 탓이다.
  **이 버그는 0.42458 제출본에도 있었다** → 제거는 순이득
- 프롬프트 억제 4블록: "기본값 0" 편향 · v15~v18 법정예외 · v9 배제조건 · v24 배제조건

**남긴 것**
- `소기업` 정규식 버그 수정 — `중소기업기본법` 의 부분문자열에 걸려 dev 107건을 오판했다. 재현율 +
- `_repair_truncated` — 절단된 JSON 을 살린다. 그룹 전체가 0 이 되는 것을 막는다. 재현율 +
- `compare._AMT` 확대 — v24 억제이지만 "인용 금액 == 등록값이면 상이가 아니다"는 정의상 참
- **v21 공동수급 최소지분율을 사실로 제공** (아래)

**죽은 코드로 남긴 것** (제출 동작에 영향 없음)
- `pipeline.verify()` / `prompts.VERIFY_*` — `--verify` 는 기본 꺼짐. **켜지 말 것** (dev −0.095)
- `gating._세부품명_미등록` / `_직생요구_있음` — 경고 주석과 함께 보존. 다시 켜지 말 것
- `presence._중소기업` 확대 — `size_restrictions` 가 파이프라인에서 더 이상 쓰이지 않아 무영향

### v21 은 강제하지 않고 사실만 준다 (2026-09-07 구현)
`law.공동_최소지분율_하한()` + `compare.joint_share_check()` → G3 프롬프트에 주입.
`v23` 일정검산·`v9` 모델명후보와 같은 그룹 조건부 블록이라 다른 그룹의 토큰을 먹지 않는다.

모델은 v21 기준("법정 하한보다 낮으면 위반")은 받지만 **하한값을 몰랐다.**
지방 5% / 국가 10%, 20% 가감 허용 → 실효 하한 4% / 8%. 조문에서 계산되는 값이다.
(근거: 국가 (계약예규) 공동계약운용요령 제9조⑤ / 지방 집행기준 제6장 제2절 1-나-2))

**강제하지 않은 이유** — dev v21 양성 6건 중 **근거문구가 있는 것은 1건뿐**이다.
```
PPS-DEV-25  "…(최소 지분율 3% 이상)"        ← 3% < 4% 하한. 규칙으로 잡힌다
PPS-DEV-049 / 055 / 056 / 058 / 059         ← 정답 근거문구 전부 빈칸
```
v21 은 항목표상 부재탐지 항목이 **아니다**(`부재탐지항목: v10 v11 v16 v18 v20`).
그런데 근거가 없다 → 나머지 5건의 위반 형태를 아직 규명하지 못했다.
6건 중 1건만 설명하는 규칙으로 강제하면 나머지를 0 으로 덮어써 재현율을 잃는다.
LLM 은 이미 정밀도 1.00 · 재현율 0.33 이다. 덮어쓸 근거가 없다.
→ `tools/probe_v21.py` (LLM 호출 없음) 로 나머지 5건을 규명한 뒤 재검토할 것.

### v1 · v21 — 놓침 8건 전부 "정답 근거문구가 빈칸"
```
v1  놓침 4건 (PPS-DEV-046, 058, 063, 072) — 정답 e1  전부 ''
v21 놓침 4건 (PPS-DEV-049, 055, 056, 058) — 정답 e21 전부 ''
```
게이팅은 통과했고 입력에도 들어갔는데 정답 라벨의 근거문구가 비어 있다.
둘 다 부재탐지 항목이 아닌데도 근거가 없다 → 라벨러가 **본문 인용이 불가능한
방식으로 위반을 판단**했다는 뜻이다 (메타데이터 기반이거나 여러 조항을 종합했거나).
즉 이 8건은 **본문에서 근거를 찾는 현재 구조로는 원리적으로 못 잡는다.**
정밀도가 1.00 이므로 재현율만 올리면 순이득이다 (v1 · v21 모두 재현율 0.33~0.43).

### v24 — 최악 항목 (정밀도 0.14~0.16)
프롬프트 배제조건으로 3회 시도해 실패했고, 09-07 제출에서 넣었던 배제조건도 제거했다.
단순 규칙(본문 계약방법·낙찰방법 추출 후 메타와 비교)도 정밀도 0.12 였다 —
본문에서 계약방법을 뽑을 때 상투 문구에 걸리는 것이 원인이다.
참가자격 절이 아니라 **'입찰에 부치는 사항' 절에 한정**해 추출해야 한다.

---

## ⛔ 실패한 접근 — 반복하지 말 것

| 시도 | 결과 | 왜 안 됐나 |
|---|---|---|
| 기저율 편향 프롬프트("기본값 0") | 예측 206→187, 상한 −0.004 | 지시로 모델 성향이 안 바뀐다 |
| 항목별 배제조건 명시 | dev −0.010 | 거짓양성과 함께 진짜양성도 줄었다 |
| **2단계 검증(재질의)** | dev −0.095, 상한 **−0.063** | 취소 80건 중 진짜양성 28건. 선별도 65%로 무작위(18%)보다는 낫지만, 같은 모델이 같은 오류를 반복한다 |
| 근거문구 없으면 억제 | dev −0.009 | 규칙 강제 판정(v23)이 근거가 없어 같이 죽는다 |
| 바이그램 품명 유사도 | 정밀도 0.14 | '통학차량 임차'→'교통신호등' |
| v14/v17/v18 물품 게이팅 | — | **항목명의 "일반물품"은 업무구분이 아니다.** v18 양성 7건 중 5건이 일반용역. 조문(영 21①10호)이 "물품 또는 용역"이라 쓴다 |
| **세부품명 미등록 게이팅 (v10·v11·v13)** | **리더보드 −0.105** | dev 상관관계를 조문으로 착각했다. 09-07 제출로 반증 |
| **v9 `is_물품` 게이팅** | 위와 같은 제출에 포함 | 위의 "일반물품 ≠ 업무구분" 실수를 반복 |
| `_rule_hints` v10·v11 강제 0 | dev 양성 6건 억제 | '직접생산'이 계약조건 상투문구로 200건 중 133건에 등장 |

**공통 패턴 — 실패한 9가지가 전부 억제(예측 줄이기)다.**
성공한 유일한 변경 v23(F1 0.000 → 0.909)은 **예측을 만들어낸** 변경이었다.
09-07 제출이 이걸 리더보드에서 확인해줬다: 억제 = −0.105.
→ **앞으로 억제는 리더보드로 검증되기 전에는 넣지 않는다.**

**유일한 성공은 v23(F1 0.000 → 0.909)** — 조문을 특정해 결정론적으로 구현한 것이다.
프롬프트 엔지니어링 5회 시도 5회 실패, 조문 구현 1회 시도 1회 성공.
→ **남은 약한 항목도 조문을 찾아 규칙으로 구현하는 것이 유일하게 검증된 경로다.**

### 고친 실제 버그
- `소기업` 정규식이 **`중소기업기본법`의 부분문자열에 걸렸다.** dev 200건 중 107건이
  "소기업 제한 있음"으로 오판되어 v18 의 진짜 양성을 규칙이 억제했다.
  → 부정 후방탐색으로 수정. v18 0.476→0.588, v17 0.200→0.364 (예측 감소, 재현율 유지)
- `calibrate.py` 가 재현율 0 을 1.0 으로 갈음해 상한을 부풀렸다.

## 측정 이력

| 라운드 | 대상 | Macro F1 | 변경 |
|---|---|---|---|
| dev20 | 20건/16항목 | 0.6125 | 첫 측정 |
| dev40a | 40건 | 0.6232 | 오답 6종 수정 |
| dev40b | 40건 | 0.6732 | 조항호 오염 제거 |
| **dev200** | **200건** | **0.6282** | v23 규칙 + 지역 단서 |
| **dev200b** | 200건 | **0.6326** | gosimatch + v9 물품게이팅 (+0.004, 사실상 무변화) |
| dev200c | 200건 | 측정 중 | 기본값0 편향 + 배제조건 |
| dev200c/d/e/f | 200건 | — | 기본값0 편향 · 배제조건 · 2단계 검증 (전부 실패, 위 표) |
| **dev200g** | 200건 | **0.6455** | 세부품명 게이팅 ON → **제출해서 0.31943 (실패)** |
| **dev200h** | 200건 | **0.6187** | 게이팅 되돌림. ⚠️ API 장애로 **빈 출력 58건** 오염 — v20 이 0.727→0.286 으로 붕괴한 건 코드 탓이 아니다. 재측정 필요 |
| holdout | 200건 | — | 라벨 없음. 예측률만 잼 |

**제출 2회**: 09-06 → **0.42458** · 09-07 → **0.31943**

### dev 는 리더보드 방향조차 못 맞힌다 (실측)
dev200b(0.6326) → dev200g(0.6455) 는 **+0.013**,
같은 두 코드의 리더보드는 0.42458 → 0.31943 으로 **−0.105**. 부호가 반대다.
**dev 로 제출 여부를 결정하면 안 된다.**
dev 로 볼 수 있는 것은 "이 변경이 dev 진짜 양성을 죽이는가"(재현율) 하나뿐이고,
그것도 항목당 양성 5~8건이라 15% 이하 손실은 안 보인다.

### 소규모 측정은 믿지 말 것
항목당 양성이 6~8건뿐이라 200건에서도 항목별 F1 이 ±0.15 흔들린다.
dev40 에서 v9 이 0.000→1.000 이라 성공으로 봤는데 dev200 에서는 0.300 이었다(예측 14/정답 6).
**항목 하나씩 붙잡는 방식은 수렴하지 않는다. 전 항목에 걸리는 체계적 변경만 신뢰할 것.**

---

## 구조

```
pps/
  law.py        조문 임계값 (고시금액·판로지원 밴드·실적배수·SW하한) — 전부 법령패키지 근거
  gating.py     항목별 적용조건 — 금액밴드·계약법 등 조문이 적용대상을 한정하는 것만 남김
                (세부품명·물품구분 게이트는 09-07 제출로 반증되어 끔. 함수는 경고와 함께 보존)
  sections.py   관련 절만 압축 (3,000자에서 근거 포함률 90.7%, 베이스라인 79.6%)
  presence.py   부재탐지 전체문서 스캔 (절단 없음)
  gosimatch.py  중기부고시 경쟁제품 대조 — 용역 카탈로그 29개를 프롬프트에 직접 제공
  compare.py    공고문↔나라장터 값 대조, 조항호 해석, 공동수급 지분율 하한 계산(v21)
  schedule.py   설명회 일정 검산 (v23, 규칙 F1 0.909)
  spec.py       규격서 모델명 후보 추출 (v9)
  prompts.py    4개 그룹 프롬프트 + 2단계 검증 프롬프트
  pipeline.py   게이팅 → 그룹 판정 → 2단계 검증 → 규칙 결합 → 근거 정합
  runner.py     Mock / API(개발전용, 제출시 제거) / vLLM
tools/
  evaluate.py   dev 채점        audit_gates.py  게이팅 안전성
  calibrate.py  예측률 상한     analyze_errors.py 오답 원인 분류
  probe_*.py    섹션·부재탐지 검증, probe_v21.py 공동수급 지분율 원문 조사(LLM 호출 없음)
build_submit.py  submit.zip 생성 (외부 API 코드 제거 검증 포함)
```

### 확립한 원칙
1. **규칙은 LLM 보다 확실히 나을 때만 덮어쓴다.** 아니면 사실로만 제공한다.
   v23: 규칙 F1 0.909 vs LLM 0.000 → 강제. v6: 규칙 F1 0.32 → 단서로만 제공.
2. **게이팅은 조문에서만 유도한다.** dev 상관관계로 막지 않는다.
   유일한 예외가 v9 물품 게이팅(dev 6/6 관찰, 조문 근거 없음) — 위험으로 표시.
3. **정규식으로 의미 매칭을 흉내내지 않는다.** 후보만 좁히고 판단은 LLM 에 맡긴다.
   (바이그램 매칭이 '통학차량 임차'를 '교통신호등'에 붙였다. 정밀도 0.14)
4. **부재탐지는 정규식이 낫다.** "문서 어디에도 없음"은 전수검사 문제이고
   LLM 은 긴 문맥에서 놓친 것을 '없다'고 답한다(MRCR 44.1%).

---

## 다음 할 일

1. **dev200d 결과 확인** — 2단계 검증이 예측률을 얼마나 낮추는지가 핵심.
   `tools/calibrate.py` 로 상한이 0.49 → 얼마로 오르는지 본다.
2. **holdout 예측률** — 실제 분포에서 우리가 얼마나 찍는지. dev 예측률보다 높으면
   밴드 A 편중(71.7%) 때문이며 v17·v18 을 우선 손봐야 한다.
3. **v24 재설계** — 예측률 12%, 정밀도 0.13. 거짓양성이 전부 *다른 항목 소관*
   (지역제한·기업규모·실적 요건을 v24 로 찍음). 배제조건을 넣었으니 효과 확인.
4. 남는 시간에 **자가 라벨링** (규칙 2-2 허용) — 홀드아웃에 라벨을 붙여 진짜 검증셋 확보.

## 실행 방법

```bash
source .env    # PPS_API_BASE/KEY/MODEL
python3 tools/evaluate.py --runner api --verify --save out/x_pred.csv
python3 tools/calibrate.py out/x_pred.csv --true-rate 0.02
python3 tools/analyze_errors.py out/x_pred.csv
python3 build_submit.py --smoke
```

주의: `| tail` 로 파이프하면 출력이 버퍼링돼 진행이 안 보인다. 리다이렉트 후 tail 할 것.
노트북 뚜껑을 덮으면 절전으로 측정이 멈춘다(`caffeinate` 로도 못 막는다).
