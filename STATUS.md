# 진행 상황 — 2026-09-07

## ⛔ 폐기된 결론: "예측량이 병목이다"

09-06 에 세운 이 가설로 하루를 썼고, **09-07 제출이 0.42458 → 0.31943 으로 떨어뜨려
반증됐다.** 아래에 증거를 남긴다. 다시 이 방향으로 가지 말 것.

## ✅ 실측된 결론: 우리는 **재현율**이 병목이다

제출 2회로 얻은 통제된 비교다. 두 제출의 차이는 **전부 억제 방향**이었다
(게이트 5개 강화 · 프롬프트 배제조건 추가 · 중소기업 정규식 확대로 v11 억제 강화).

| 제출 | 커밋 | 게이팅 차단율(실제분포) | 오프라인 상한 | dev F1 | **리더보드** |
|---|---|---|---|---|---|
| 09-06 | `24ccf2d` | 31.2% | 0.4883 | 0.6326 | **0.42458** (205/748) |
| 09-07 | `3471ccf` | **40.5%** | 0.5041 ↑ | 0.6455 ↑ | **0.31943** ↓ |
| 09-08 | (미커밋) | 31.2% | — | **0.6758** | **0.47452** ✅ 최고 |

## ✅ dev→실제 대응비가 회복됐다 — **dev 를 다시 신뢰할 수 있다**

```
제출     설정        dev      실제      대응비   비고
09-06   24ccf2d   0.6326   0.42458   0.671   게이팅 보통
09-07   3471ccf   0.6455   0.31943   0.495   게이팅·억제 강화 → 붕괴
09-08   dev200l   0.6758   0.47452   0.702   억제 추가 0   ← 최고 대응비
```

**대응비가 0.671 → 0.495 → 0.702 로 움직였다. 억제를 뺄수록 전이가 좋아진다.**
이건 단순히 "억제가 dev 점수를 깎는다"가 아니다 — 억제는 **dev 보다 실제에서 더 크게 깎는다.**
dev 는 항목당 양성 5~8 건으로 인위적 균형이라 억제의 재현율 손실이 축소돼 보이고,
실제 평가셋에서는 그 손실이 그대로 드러나기 때문이다.
→ **앞으로 억제는 리더보드 검증 없이 넣지 않는다**는 원칙이 세 점으로 확증됐다.

**실무적 함의: 오프라인 반복이 가능해졌다.**
`dev × 0.70 ≈ 실제` 가 성립하고, 같은 시드·같은 프롬프트면 등급이 **완전히 재현**된다
(프롬프트 동일 항목 2,332칸 변동 0). 즉 프롬프트 A/B 는 **dev200 1회로 인과가 확정**된다.
제출은 검증이 아니라 **확인** 용도로 아껴 쓸 수 있다.

**남은 여력 (대응비 0.702 기준)**
```
실제 0.55 → dev 0.783 필요
실제 0.60 → dev 0.855 필요
실제 0.65 → dev 0.926 필요
1등 0.744 → dev 1.059 필요 (불가능)
```
현재 dev 0.6758. **목표 0.8 은 이 구조로 도달 불가**임이 수치로 확정됐다.
현실적 상한은 dev 를 0.85 근처까지 올려 **실제 0.60** 을 노리는 것이고,
그러려면 지금 F1 0.5 미만인 항목(v9 0.348 · v13 0.400 · v17 0.500 · v18 0.296 ·
v19 0.500 · v24 0.235)에서 대부분을 0.7 이상으로 올려야 한다.

**예측 칸을 9.3%p 줄였더니 점수가 0.105 떨어졌다.**
dev 도 상한도 "좋아졌다"고 말했는데 실제는 크게 나빠졌다.
→ 잘라낸 칸에 거짓양성보다 **진짜 양성이 더 많았다**는 뜻이다.

### 오프라인 지표 두 개가 모두 실패했다 — 왜인지가 중요하다

**`calibrate.py` 의 상한은 억제를 평가하는 데 쓸 수 없다.**
상한은 `정밀도 ≤ 실제양성률/예측률` 로 계산하고 **재현율은 dev 값을 그대로 쓴다.**
게이팅은 정의상 예측률을 낮추므로 상한이 **기계적으로 올라간다.**
그러면서 게이팅이 죽인 진짜 양성은 dev 에서 안 보이면 반영되지 않는다.
→ **억제 변경을 상한으로 평가하면 항상 좋아 보인다. 구조적으로 눈이 먼 지표다.**

**dev 도 이 변경을 걸러내지 못했다** (+0.013 이라고 말했다).
"게이팅이 죽인 진짜 양성 0건"의 근거였던 dev 양성은 19건뿐이다.
0/19 의 95% 신뢰상한은 15.8%(rule of three) — **0건 관측은 안전의 증거가 아니다.**

### 게이트가 실제 분포에서 더 막은 것도 아니다 (측정함)

| 게이트 통과율 | dev200 | holdout200(실제분포) |
|---|---|---|
| 세부품명 미등록 (v10·v11·v13) | 61.0% | 62.5% |
| 업무구분 물품 (v9) | 39.0% | 37.5% |
| 직생요구 문구 존재 (v12) | 66.5% | 53.5% |

분포 차이는 거의 없다. 즉 **게이트 자체가 진짜 양성을 자른 것**이지,
"dev 에만 맞는 게이트였다"는 설명이 아니다. 조문에서 유도했다는 근거도
실제로는 조문이 아니라 **dev 상관관계**였다 — 세부품명 등록 여부는
어느 조문에도 나오지 않는다.

### 놓침의 원인은 하나로 모인다 (dev200g 분석)

`tools/analyze_errors.py` — 놓침 51건의 원인 분류:
```
모델오판 50건 · 입력누락 1건 · 게이팅차단 0건
```
배관(게이팅·섹션·규칙)은 멀쩡하다. **근거가 프롬프트에 들어갔는데 모델이 0 이라 답한다.**

그리고 놓침 51건 중 **42건(82%)이 "그룹 전체 침묵"** — 같은 그룹의 다른 항목도
전부 0 이었다. 형제 항목 혼동(9건, 18%)이 아니다.
→ 항목 정의를 다듬는 문제가 아니라 **판정 문턱 문제**다. 전 항목에 걸린다.

**v20 은 약체가 아니다.** dev200h 의 v20 재현율 0.20 은 API 장애로 생긴
빈 출력 58건 탓이다. 깨끗한 dev200g 에서는 0.80 이다. 최약체 목록에서 빼라.

---

## dev 는 평가 분포를 대표하지 않는다 (실측)

`train_unlabeled` 20,000 건과 견주면 dev 는 **큰 계약에 편중**돼 있다.

| 판로지원 밴드 | dev | 실제 20,000건 |
|---|---|---|
| A (1억 미만) | 53.0% | **71.7%** |
| B (1억~고시금액) | 24.5% | 17.1% |
| C (고시금액 이상) | 22.5% | **11.2%** |

그 결과 **노력 배분이 틀렸었다**. 게이팅 통과율 = 그 항목이 적용되는 공고 비율:

| 항목 | dev | 실제 | 우리 F1 | |
|---|---|---|---|---|
| v17 | 53.0% | **71.7%** | 0.400 | 실제로 훨씬 중요 |
| v18 | 53.0% | **71.7%** | 0.333 | 실제로 훨씬 중요 |
| v23 | 20.5% | **5.8%** | 0.909 | dev 에서 3.5배 부풀려짐 |
| v22 | 42.5% | 24.3% | 0.889 | |
| v8 | 85.5% | 60.3% | 0.706 | |

**v17·v18 은 실제 공고의 72%에 적용되는데 우리 최약체다.** 정밀도 0.24~0.50 이라
실제 분포에서 거짓양성을 대량 생산한다.

---

## 데이터 역할 분리 (이번에 확립)

dev 하나로 튜닝과 검증을 겸하면 안 된다.

| 데이터 | 역할 | 라벨 |
|---|---|---|
| `dev.jsonl.gz` 200건 | 판정 **정확성** 검증 | 있음 |
| `train_unlabeled` 20,000건 | 예측률 **분포 보정** | 불필요 |
| `out/holdout200.jsonl.gz` | 실제분포 층화표본 홀드아웃 | 없음(예측률만) |

**예측률은 정답 없이도 잰다** — 다만 그것으로 상한을 계산하려면 실제 양성률을 알아야 하는데
**우리는 모른다.** `--true-rate 0.02` 는 근거 없는 가정이었고, 그 위에 세운 상한이
09-07 제출을 잘못 승인했다. 예측률은 **관찰값으로만** 쓰고 의사결정에는 쓰지 않는다.

### dev 는 인위적으로 균형 잡힌 표본이다 (이번에 확인)
항목별 양성이 **전부 5~8건**이다. 24개 서로 다른 위반유형이 우연히 이렇게 고를 수 없다.
```
v1 7 · v2 7 · v3 8 · v4 6 · v5 7 · v6 6 · v7 7 · v8 6 · v9 6 · v10 7 · v11 6 · v12 6
v13 6 · v14 8 · v15 6 · v16 6 · v17 6 · v18 7 · v19 6 · v20 5 · v21 6 · v22 5 · v23 5 · v24 8
평균 양성률 3.19%   (STATUS 가정치였던 2.00% 가 아니다)
```
평가셋에서 희귀한 항목은 양성이 훨씬 적어 F1 이 무너진다. **dev 는 수준을 못 맞춘다**
(dev 0.63 ↔ 실제 0.42). 밴드 편중까지 겹친다: dev A 53.0% vs holdout200 A 71.5%.

`out/holdout200.jsonl.gz` 는 20,000 건에서 밴드·계약법·업무구분 비율을 유지해 뽑은 200건이다
(모집단과 오차 1%p 이내).

---

## ⛔ 반증된 경로: 조문·정의 기반 게이팅 (2026-09-07 제출로 폐기)

경쟁제품 4형제(v10·v11·v12·v13)와 v9 를 게이팅했다.
dev 는 0.6326 → 0.6455, 상한은 0.4883 → 0.5041 로 올랐다.
**리더보드는 0.42458 → 0.31943 이었다.** 되돌렸다.

### 무엇이 틀렸나
`meta.세부품명번호목록` 미등록 게이트의 근거는 "dev 양성 19건이 전부 미등록"이었다.
이건 **조문이 아니라 dev 상관관계다.** 세부품명 등록 여부는 판로지원법 어디에도 없다.
등록률이 dev 39.0% ≈ train_unlabeled 37.6% 라는 '분포 검증'은
**게이트가 진짜 양성을 자르는지와 무관한 검사였다** — 통과해도 아무것도 보증하지 않는다.

v9 의 `is_물품` 게이트는 이미 알고 있던 실수를 반복한 것이다
(아래 실패표: "항목명의 '일반물품'은 업무구분이 아니다").

**게이팅 채택 기준을 다시 쓴다** — 아래를 전부 만족할 때만 켠다.
1. 조문이 **적용 대상 자체를 한정**할 것 (예: 금액 밴드, 계약법). 메타 필드의 존재 여부는 근거가 아니다.
2. 게이트가 없으면 그 항목이 **논리적으로 성립 불가**할 것.
3. dev 에서 "죽인 양성 0건"은 **필요조건일 뿐 충분조건이 아니다** (양성 6~8건 → 15% 오차).

---

## 09-08 제출 후보 = 09-06 기준선 + 검증된 버그 수정 + v21 사실제공

커밋 안 된 작업트리 상태다. 억제는 **하나도** 넣지 않았다.

**되돌린 것** (전부 억제 방향)
- 게이트 5개(v9·v10·v11·v12·v13) → 전부 `_always`.
  **게이트 함수가 기준선과 완전히 일치함을 diff 로 확인했다**
- `_rule_hints` 의 v10·v11 강제 0 — dev 에서 v10 양성 7건 중 4건, v11 6건 중 2건을 죽이고 있었다.
  '직접생산'이 계약조건 상투문구로 200건 중 133건에 등장하는 탓이다.
  **이 버그는 0.42458 제출본에도 있었다** → 제거는 순이득
- 프롬프트 억제 4블록: "기본값 0" 편향 · v15~v18 법정예외 · v9 배제조건 · v24 배제조건

**남긴 것**
- `소기업` 정규식 버그 수정 — `중소기업기본법` 의 부분문자열에 걸려 dev 107건을 오판했다. 재현율 +
- `_repair_truncated` — 절단된 JSON 을 살린다. 그룹 전체가 0 이 되는 것을 막는다. 재현율 +
- `compare._AMT` 확대 — v24 억제이지만 "인용 금액 == 등록값이면 상이가 아니다"는 정의상 참
- **v21 공동수급 최소지분율을 사실로 제공** (아래)

### ⛔ 결손 항목 392칸은 **재현율 누출이 아니라 dev 계측 오차였다**

한때 `max_tokens` 1200 이 좁아서 JSON 이 절단된다고 보고 2000 으로 올렸다.
**틀렸다.** `.cache/api` 1,200건을 직접 열어 반증했고 1200 으로 되돌렸다.

```
정상 응답 1,081건 — 길이 중앙 149자 · 90% 318자 · **최대 580자**
절단   119건(9.9%) — **전부 200자 미만, 대부분 1~9자**   예: '{\n  "v12":'
```

정상 응답이 최대 580자다 → **1200 토큰 상한에 닿은 적이 없다.**
절단된 119건은 1~9자 조각이니 토큰 상한이 아니라 **연결 끊김**이다.
제출 경로는 로컬 vLLM 이라 네트워크가 없다 → **제출본에는 이 문제가 없다.**

**함의 두 가지 (이게 더 중요하다)**
1. dev 측정은 그룹 호출의 약 10%를 잃은 상태였다 → **우리 dev 점수는 실제보다 낮게 나왔다.**
   dev200h 의 v20 붕괴(0.727 → 0.286)가 그 예다.
2. "놓침의 82% 가 그룹 전체 침묵" 중 약 12건(24%)은 **모델 판단이 아니라 끊긴 호출**이다.
   실제 침묵은 42건이 아니라 ~30건(59%)이다. **문턱 문제는 추정보다 작다.**

**실제 고친 것** — `runner.ApiRunner._looks_truncated()` + 재시도 (개발 전용, 제출본에서 제거됨).
200 응답인데 본문이 끊긴 경우를 재시도한다. 판정은 보수적으로 "JSON 으로 시작 +
파싱 실패 + 200자 미만"일 때만. 단위 테스트 6/6 통과.
→ 다음 dev 측정은 결손이 크게 줄어야 한다. 줄지 않으면 원인이 또 다른 것이다.

⚠️ `.cache/api` 는 어차피 무효다 — `SYSTEM` 프롬프트에서 '기본값 0' 문단을 뺐기 때문에
   전 그룹의 캐시 키가 바뀌었다. dev 재측정은 실제 API 호출 800건이 필요하다.

**같이 넣은 방어 — `prompts._safe()`**
`Pipeline.render()` 는 `tasks = [self.render(t) for t in self.plan(recs)]` 로
**추론을 시작하기 전에 전 레코드에 대해** 호출된다. 따라서 보조 사실 블록
(`fact_block` · `gosimatch.block` · `schedule.fact_line` · `spec.candidate_block` ·
`compare.joint_share_check`) 중 하나가 어떤 한 건에서 예외를 던지면
`pipe.run` 이 통째로 실패하고 `build_submit` MAIN 의 예외 경로를 타 **전 항목 0 이 제출된다.**
1,853건을 2시간 돌린 뒤 그렇게 되면 그날 제출 기회를 잃는다.
→ 다섯 블록을 전부 `_safe()` 로 감쌌다. 실패하면 그 블록만 빠지고 나머지는 그대로 간다.
동작 변화는 없다(예외가 없을 때는 이전과 동일한 문자열).

---

## ⚠️ 이 트리는 아직 실행 검증이 안 됐다 — 제출 전 반드시

### 📉 09-08 예측: **0.45 ~ 0.48** (1등 0.74393)

신뢰할 앵커는 리더보드 2점뿐이다.

| 제출 | dev | 실제 | dev→실제 비율 |
|---|---|---|---|
| 24ccf2d 기준선 | 0.6326 | 0.42458 | **0.671** |
| 3471ccf 억제판 | 0.6455 | 0.31943 | 0.495 (붕괴) |

억제판의 비율이 무너진 건 게이팅이 dev 에 안 보이는 방식으로 재현율을 깼기 때문이다.
현재 빌드는 새 게이팅·억제가 없으므로 기준선의 0.671 에 가까울 것으로 본다.
추정 dev 0.67~0.71 (dev200h 0.6187 정상화 ~0.63 + v21 +0.021 + 계측오차 회복)
× 0.671 = **0.45~0.48**.

**즉 이번 제출로 0.6 은 불가능하다.** 기대할 수 있는 건 "재현율이 병목"이라는
가설의 확정과 기준선 대비 +0.03 정도다. 0.6 을 넘으려면 dev 0.90 이 필요하고
현재 dev 는 0.65 다 — 구조적으로 다른 접근이 필요하다.

✅ **검증 완료 (2026-09-07)** — API 호출 없이 오프라인으로만 확인했다.

```
_looks_truncated 단위테스트                   → 6/6 PASS
python3 tools/probe_v21.py                    → v21 양성 6건 원문 확인
python3 tools/evaluate.py --runner mock       → v21 F1 1.000 · v23 F1 0.909 · 게이팅 27.4% · 결손 0
python3 build_submit.py --smoke               → 자가검증 PASS · 49열 · submit.zip 84,210 bytes
제출본 zip 직접 검사                          → 금지어 5종 0건 · max_tokens 1200 · check_v21 포함 · 하한 잔재 없음
```

빌드가 실제로 걸러낸 것: `pipeline.py` 주석에 `ApiRunner` 라고 쓰자 금지어 검사가
빌드를 중단시켰다. **주석에도 금지어를 쓰면 안 된다** — 문자열 검사라 문맥을 안 본다.

mock 러너는 LLM 판정을 내지 않으므로 **규칙 레이어만의 성적**이 그대로 보인다.
그 상태에서 v21 이 TP 6 · FP 0 · FN 0 이었다 — 규칙이 `finalize` 까지 정상 배선됐다는 뜻이다.
게이팅 차단 27.4% 도 09-06 기준선과 정확히 일치한다.

아직 안 한 것: **dev200 실측(실제 LLM)**. `max_tokens` 변경으로 `.cache/api` 가 전부
무효화되어 재측정은 실제 API 호출 800건이 필요하다. 결손 항목이 실제로 줄었는지는 미확인.

**죽은 코드로 남긴 것** (제출 동작에 영향 없음)
- `pipeline.verify()` / `prompts.VERIFY_*` — `--verify` 는 기본 꺼짐. **켜지 말 것** (dev −0.095)
- `gating._세부품명_미등록` / `_직생요구_있음` — 경고 주석과 함께 보존. 다시 켜지 말 것
- `presence._중소기업` 확대 — `size_restrictions` 가 파이프라인에서 더 이상 쓰이지 않아 무영향

### ✅ v21 — 규칙 강제. dev F1 1.000 (2026-09-07)

v23 이후 **두 번째로 성공한 조문 구현**이다. `compare.check_v21()` → `pipeline._rule_hints`.

```
규칙 TP 6 · FP 0 · FN 0 → F1 1.000    (같은 항목 LLM 은 F1 0.500 · 재현율 0.333)
확답 27건 / 보류 173건, 보류 중 정답 양성 0건
```

**판정**: 공고가 정한 구성원별 최소지분율 < 조문 기준(지방 5% / 국가 10%) → 위반.
근거: 국가 (계약예규) 공동계약운용요령 제9조⑤ / 지방 집행기준 제6장 제2절 1-나-2).

**⛔ "20% 범위 가감" 단서를 적용하면 안 된다** — 실측으로 갈렸다.
```
정액 기준(지방 5 / 국가 10) : TP 6 · FP 0 · FN 0 → F1 1.000
가감 하한(지방 4 / 국가  8) : TP 5 · FP 0 · FN 1 → F1 0.909
   놓친 것: PPS-DEV-049 지방·최소지분율 4.0% (4.0 < 4.0 = 거짓)
```
항목명이 **"공동 5% (10%)"** 인 것과 일치한다. 채점 기준은 원값이다.
오용을 막기 위해 `공동_최소지분율_하한()` 과 `*_하한` 상수는 아예 두지 않았다.

**보류(None)하는 경우** — 규칙이 답하지 않고 LLM 에 넘긴다.
- 지분율 표기를 못 찾음 → '부재'를 위반으로 보지 않는다. v21 은 부재탐지 항목이 아니다.
- 분담이행 전용 → 조문상 최소지분율 미적용. 단 공동이행 표기가 함께 있으면 보류하지 않는다
  (두 방식을 같이 허용하는 공고가 흔해서, '분담이행' 표기만으로 보류하면 위반을 놓친다).

정규식은 **후보 좁히기**만 한다(원칙 3). 지분율 수치 주변 120자에 공동수급 문맥
단서가 있는 것만 남기고, 인용구를 함께 넘겨 프롬프트에도 같은 사실을 준다.

### ⚠️ 정정 — "정답 근거문구가 빈칸이면 본문에서 못 잡는다"는 **틀렸다**

09-06 에 이렇게 적었다: v1·v21 놓침 8건의 정답 근거가 전부 빈칸이므로
"라벨러가 본문 인용이 불가능한 방식으로 판단했다 → 현재 구조로는 원리적으로 못 잡는다."

**v21 에서 반증됐다.** 근거가 빈칸인 5건의 원문을 직접 읽어 보니 지분율이 또박또박 적혀 있었다.
```
PPS-DEV-049 "구성원별 최소지분율은 4% 이상이어야 합니다"
PPS-DEV-055 "구성원별 최소 지분율은 5% 이상으로 하여야 함"
PPS-DEV-056 "구성원별 최소 지분율은 0.5% 이상으로 하여야 함"
PPS-DEV-058 "구성원별 계약참여 최소 지분율은 5%이상으로 하여야 함"
PPS-DEV-059 "업체별 최소 지분율은 2% 이상, 구성원 수는 5인 이하"
```
**라벨러가 인용을 생략한 것이지 근거가 없는 것이 아니었다.**
빈 `e{n}` 은 "본문에 근거가 없다"는 뜻이 아니라 그냥 **라벨 품질 문제**다.

→ **v1 의 놓침 4건(PPS-DEV-046, 058, 063, 072)도 같은 방식으로 원문을 읽어야 한다.**
포기할 근거가 사라졌다. v1 은 정밀도 1.00 · 재현율 0.43 이라 순이득 구간이다.
`tools/probe_v21.py` 를 본떠 만들면 API 없이 조사할 수 있다.

### v24 — 최악 항목 (정밀도 0.14~0.16)
프롬프트 배제조건으로 3회 시도해 실패했고, 09-07 제출에서 넣었던 배제조건도 제거했다.
단순 규칙(본문 계약방법·낙찰방법 추출 후 메타와 비교)도 정밀도 0.12 였다 —
본문에서 계약방법을 뽑을 때 상투 문구에 걸리는 것이 원인이다.
참가자격 절이 아니라 **'입찰에 부치는 사항' 절에 한정**해 추출해야 한다.

---

## ⛔ notice-level 2단계 구조 — **상한은 크지만 도달 불가. API 미실행** (2026-09-09)

"정답 양성 0 인 공고를 먼저 걸러내고 24항목을 판정한다"는 2단계 구조를 오프라인으로 검증했다.

### 상한은 크다
```
현재 dev200n                    TP 127 FP 121 FN 26 · 정밀도 0.512 · Macro 0.6850
완벽 게이트(정답0 공고 112건 전부 0)  TP 127 FP  59 FN 26 · 정밀도 0.683 · Macro **0.7686**
                                                                      상한 이득 +0.0836
```

### 그러나 손익 구조가 가혹하다
```
정답0 공고를 0 처리   → FP 0.55건 절약 · TP 손실 0
정답≥1 공고를 0 처리  → FP 0.67건 절약 · **TP 1.44건 손실**
→ 오분류 1건의 대가가 정분류 1건 이득의 **2.6배**
```

### 모델 자신의 판정으로는 가릴 수 없다
예측 양성 개수별 정답0 비율과 0 처리 손익:
```
예측+ 0건  74공고 · 정답0 93%  → FP −0  TP −0    (이미 0)
예측+ 1건  60공고 · 정답0 50%  → FP −32 TP −28   ❌ 비율 1.14 (필요 2.6)
예측+ 2건  31공고 · 정답0 26%  → FP −30 TP −32   ❌
예측+ 3건  23공고 · 정답0 17%  → FP −41 TP −28   ❌
예측+ 4건+ 12공고 · 정답0  8%  → FP −18 TP −39   ❌
```
**예측 양성이 1건인 공고는 정답0 비율이 50% — 동전던지기다.**

특징(예측수·최대등급·근거유무·그룹수)을 조합해 **규칙 18종을 전수 탐색**했다:
```
최고    npos<=2 · 근거없음   대상 16건(정답0 11) → Macro 0.6811  **−0.0040**
차선    npos<=1 · 근거없음   대상 14건(정답0  9) → Macro 0.6779  −0.0071
최악    npos<=1              대상 60건(정답0 30) → Macro 0.6095  −0.0756
```
**18종 전부 음수다.** 상한 +0.0836 과 도달 가능치 −0.0040 의 간극이 이 접근의 한계다.

### LLM 에 직접 묻는 안도 기각 (API 미실행)
"이 공고에 위반이 있는가"를 재질의하는 것은 **2단계 검증의 변형**이다.
그것은 이미 실패했다(dev −0.095 · 취소 80건 중 진짜양성 28건 · 같은 모델이 같은 오류 반복).
게다가 notice-level 은 **오류 1건당 파괴력이 24배**다 — 한 칸이 아니라 공고 전체를 0 으로 만든다.
오프라인 신호가 npos=1 에서 50/50 인데 2.6배 정확도가 필요하다 → 기대값이 명백히 음수다.
→ **"기존 best 보다 좋아질 때만 반영"** 원칙에 따라 API 를 쓰지 않았다.

## 🎯 FP 121 의 정체 — **적법한 공고를 적법하다고 못 한다** (2026-09-09 · API 0)

dev200n FP 를 v10~v18 중심으로 분해했다. **항목별 문제가 아니라 공고별 문제였다.**

### 발견 1 — FP 의 절반이 "정답 양성 0" 공고에서 나온다
```
정답 양성 0 인 공고 112건(56%) → FP 62건 = 전체 FP 121건의 51%
  이 공고들은 TP 가 원리적으로 0 이다 → **예측 자체가 전부 오류**
정답 양성 ≥1 인 공고 88건    → TP 127 · FP 59 · **정밀도 0.683**
```
전체 정밀도 0.512 인데 **위반이 실제로 있는 공고에서는 0.683 이다.**
문제는 판정 능력이 아니라 **"위반이 없다"를 말하지 못하는 것**이다.

### 발견 2 — 공고당 FP 가 내용과 무관하게 평탄하다
```
정답 양성 0개 → 공고당 FP 0.55
        1개 → 0.69
        2개 → 0.61
      3개+ → 0.69
```
**문서에 위반이 몇 개 있든 모델은 공고당 약 0.6건을 만들어낸다.**
"이 문서에 위반이 있는가"를 읽는 것이 아니라 "어느 항목을 눈에 보이는 것에 붙일까"를 한다.
→ 실제 분포 환산: 1,853건 × 0.6 ≈ **1,100 FP**. 리더보드 실제 정밀도 0.331 과 정합한다.

정답 0 인 공고 112건 중 모델도 0 으로 맞춘 것은 **69건(62%)** 뿐이다.

### 발견 3 — 어휘 신호는 **하나도** FP/TP 를 가르지 못한다
부재탐지 항목이 '없다'고 주장한 그 요건을 정확히 겨냥해 탐지했다:
```
항목  요건                FP중 존재      TP중 존재
v10   직생확인 요구        5/12 (42%)   3/5 (60%)   ← TP 쪽이 더 높다
v11   중소기업자 제한      2/9  (22%)   2/6 (33%)   ← TP 쪽이 더 높다
v16   중소기업 제한        0/5  ( 0%)   1/5 (20%)   ← TP 쪽이 더 높다
v18   소기업·소상공인 제한  3/9  (33%)   0/4 ( 0%)
v13   '중소기업' 포함      10/11(91%)   3/3 (100%)  ← 완전히 겹친다
```
**v13 이 특히 결정적이다.** 지시문은 "중기업이 포함되면 v13 이 아니다"라고 명시하는데,
FP 91% · TP 100% 가 모두 '중소기업' 문구를 갖고 있다. 어휘로는 구분 불가능하다.

### 발견 4 — 구조적 표지는 있지만 게이팅으로 못 쓴다
```
              수의계약  소액수의_지방  밴드A  추정가격 중앙
정답 0 (112건)   28%       36%      62%   81,600,000
정답 ≥1 ( 88건)   5%       23%      42%  127,363,636

수의계약 35건 · 정답0 비율 89% · 정밀도 0.286 · 공고당 FP 0.43
경쟁입찰 165건 · 정답0 비율 49% · 정밀도 0.533 · 공고당 FP 0.64
```
수의계약은 89%가 완전 적법이다. 하지만 **v18 조사에서 이미 확인했듯 원인은 수의계약이
아니라 제2조의3 예외 명시**였고, 수의계약은 상관변수였다. 게이팅으로 쓰면 09-07 재현이다.

### 결론: **API 실행하지 않음. 개선안 근거 부족**
지시대로 "문맥은 유지하되 잘못된 양성을 막는 최소한의 판단 기준"을 찾았으나,
**측정이 어떤 후보도 지지하지 않는다.**
```
어휘 기준     발견 3 — 5개 항목 전부 FP/TP 미분리. v10·v11·v16 은 TP 쪽 비율이 더 높다
구조 기준     발견 4 — 수의계약은 상관변수(v18 조사에서 이미 기각)
기저율 프롬프트  STATUS 실패표 1행 — "예측 206→187 · 지시로 모델 성향이 안 바뀐다"
```
마지막 것이 발견 1·2 의 직접 처방인데 **이미 실패한 접근**이다. 재시도는 근거가 없다.

**남은 유일한 실질 경로**: 발견 2 가 말하는 것은 "모델이 공고당 고정 비율로 위반을
만들어낸다"이고, 이는 프롬프트 지시로 안 바뀐다는 것이 이미 실측됐다.
→ 이 파이프라인 구조에서 FP 를 75% 줄이는(LB 0.75 조건) 방법을 **찾지 못했다.**

## ⛔ G2 분할 실험 — **D급. 원복함** (2026-09-09 · dev200o)

가설: G2 가 경쟁제품 계열(v10~v13)과 밴드 계열(v14~v18)을 한 창에 담아 간섭을 만든다
→ 분리하면 개선된다. **반대였다.**

독립변수를 하나로 통제했다 — `지시`·`cue`·`budget`·`full_doc`·항목지시 문구를 분할 전
파일과 기계 대조해 **완전 일치** 확인, 문서 부분 해시도 G2A·G2B 동일(30/30건),
고시대조 사실블록도 양쪽에 동일하게 실었다. 게이팅·규칙·finalize·G1/G3/G4 전부 무변경.

| | TP | FP | FN | 정밀도 | 재현율 | Macro |
|---|---|---|---|---|---|---|
| dev200n (G2 통합) | 127 | 121 | 26 | 0.512 | 0.830 | **0.6850** |
| dev200o (G2 분할) | 131 | **167** | 22 | **0.440** | 0.856 | 0.6737 |
| 변화 | +4 | **+46** | −4 | **−0.072** | +0.026 | **−0.0114** |

**TP 손실은 0건이다(C 유형 0). 재현율은 올랐다(+0.026). 그런데 FP 가 46건 폭증했다.**
```
G2A 경쟁제품  FP 33 → 76 (+43)   ← v13 이 11 → 44 (+33) 로 폭발
G2B 밴드/금액  FP 27 → 30 (+3)
G1·G3·G4      **완전 불변** ✅ (API 결정론 재확인 · G2 만 바뀌었다는 통제 증거)
```

### 이번 실험이 답한 질문
> "G2 의 경쟁제품 판단과 밴드/금액 판단을 동일 context 에서 동시에 수행하는 것이
> 실제로 성능에 악영향을 주는가?"

**아니다 — 정반대다. 합쳐 놓은 문맥이 경쟁제품 계열의 과잉 예측을 억제하고 있었다.**
v14~v18(밴드 계열)이 같은 창에 있으면 모델이 "이 공고는 일반 물품·용역 구간이다"라는
사실을 함께 읽고 v10·v13 을 자제한다. 그것을 떼어내자 v13 이 44건을 찍었다.
등급 3 이 241 → 282 로 늘어난 것이 같은 현상이다.

**문맥 간섭 가설 자체는 확인됐다** — dev200n 에서 한 방향(v16·v18 문구가 v10·v13 을 억제),
dev200o 에서 반대 방향(분리하니 억제가 사라짐). **간섭은 실재하고, 통합 형태가 순이득이었다.**

**교훈**: 프롬프트를 쪼개면 항목별 지시는 격리되지만 **판단에 도움이 되던 맥락도 함께 사라진다.**
`Group.항목지시`(지시문 격리)는 이득이었고 **그룹 분할(문맥 격리)은 손해다.** 둘은 다른 것이다.

**부수 확인**: 호출 800→1000(공고당 5회)로 늘어 평가셋 환산 9,265회/6,300초 = 초당 1.47회.
효과가 있었어도 시간예산 위험을 감수해야 했다. 이제 그 고민이 불필요하다.

**원복 완료** — `pps/prompts.py` 를 분할 전으로 되돌렸고 변경 지문이
dev200n 실행 시와 바이트 단위로 동일(`320d42d07670b557`)함을 확인했다.
호출 800회 · test_threshold PASS · 게이팅 감사 통과 · smoke PASS.

## 🔎 조문 기반 규칙 후보 탐색 (2026-09-09 · API 0 · 코드 0)

### v21·v23 이 성공한 공통 구조 (코드에서 추출)
```
① 적용조건이 메타데이터로 확정된다      v23: 지방 AND 협상 / v21: 제한 없음
② 문서에서 **레이블 붙은 단일 수치**를 뽑는다   v23: 날짜 3개 / v21: 지분율 %
③ 법령이 그 수치의 **명시적 임계값**을 준다     v23: 10·20·40일 + 7일 / v21: 5%·10%
④ 판정 = **수치 대 수치 비교**. 의미 판단이 0 이다
⑤ 추출 실패·모순이면 None 을 돌려 **LLM 에 위임**한다 (강제하지 않는다)
```
⑤가 특히 중요하다 — v23 은 `gap <= 0` 이면 "날짜 추출이 어긋난 것"으로 보고 None 을 낸다.
dev 거짓양성 7건 중 6건이 이 유형이었다. **규칙이 자기 한계를 알고 물러난다.**

### 우선순위 5개 항목 대조 결과 — **전부 구조 불일치**

| 항목 | 조문 | ②정형 수치 | ④수치비교 | 판정 |
|---|---|---|---|---|
| **v24** | **항목표: "해당 없음 — 조문 없는 대조형 항목"** | 값 6종 | 부분 | **X** |
| v9 | 집행기준 제2장 제5조 (수치 없음) | 없음 | 없음 | **X** |
| v13 | 판로지원법 제7조① (수치 없음) | 없음 | 없음 | **X** |
| v10 | 판로지원법 제9조 (수치 없음) | 없음 | 없음 | **X** |
| v18 | 시행령 제2조의2①1호 + 제2조의3 | 밴드는 계산됨 | 예외 사유 판단 | **B** |

**v24 는 항목표가 조문 없음을 명시한다.** 조문 기반 규칙이 원리적으로 불가능하고,
남는 것은 값 대조뿐인데 **이미 실측했다**(규칙 F1 0.333 vs LLM 0.258, 합집합 0.160).
→ 중복 후보. 재제안하지 않는다.

**v9·v13·v10 은 조문에 수치가 없다.** v9 는 "특정 규격·모델 지정 금지"라는 정성 규정이고,
v10·v13 은 "경쟁제품이면 직생확인/중소기업자 제한"인데 **경쟁제품 판별 자체가 의미 판단**이다.
게다가 v10·v11·v13 FP 30건 중 **28건이 "정답은 이 공고를 판단대상으로도 안 봄"** 이므로
경쟁제품 식별을 코드로 옮겨도 FP 가 줄지 않는다(이미 측정).

**v18 은 밴드(계산됨) + 예외 사유(의미 판단) 조합이다.** 예외는 항목표가
"예외를 **명시한 경우**"라고 조건을 달았으므로 사유의 타당성을 읽어야 한다.
→ 등급 B(soft fact). **이미 dev200n 에서 프롬프트로 처리해 FP 16→9 를 얻었다.**

### 구조를 만족하는 항목은 **이미 전부 구현돼 있다**
```
v5  1.000 (FP 0)  지역기준 금액 대조      v21 1.000 (FP 0)  지분율 대조 [규칙]
v3  0.941 (FP 1)  실적배수 대조           v7  0.933 (FP 1)  지역 범위 대조
v6  0.923 (FP 1)  지역 단위 대조          v23 0.833 (FP 2)  일정 일수 대조 [규칙]
```
**수치 대조 구조를 가진 6항목이 F1 상위 8위 안에 전부 들어 있고 FP 합이 5건이다.**
반면 FP 상위 5항목(v24 19·v9 13·v13 11·v10 12·v18 9)은 **하나도 이 구조가 아니다.**

### 새로 확인한 것 — v19 는 X, v3 는 이미 최적
- **v19** 항목표 비고가 직접 경고한다: **"입찰 전 발급, 계약시 제출 등 표현 다양"**.
  항목표 스스로 정형 추출이 안 된다고 말한다 → **X**. 규칙화하면 안 된다.
- **v3** 항목표 비고 "사업예산 기준" vs 시행규칙 제25조②1호나목 "**추정가격**의 1배 이내".
  조문이 추정가격을 명시하고 현재 `fact_block` 도 추정가격을 쓴다 → **현행이 조문과 일치**.
  FP 1건뿐이라 여력도 없다(+0.0024).

### 결론: **S 등급 후보 0개 · A 등급 후보 0개**

조문 기반 규칙 경로는 **소진됐다.** 근거:
```
수치 대조 구조를 가진 항목 6개 = 전부 구현 완료 · F1 0.83~1.00 · FP 합 5건
FP 상위 5개 항목            = 조문에 수치가 없거나(v9·v10·v13) 조문 자체가 없다(v24)
규칙 후보 실측 이력          13개 시도 → 2개 성공(v21·v23), 11개 실패
```
**억지로 규칙화할 후보를 만들지 않는다.** v24 를 값 대조로 강제하면 재현율 절반을 팔고
(TP 4→2) Macro 는 +0.003 이며, v10·v13 을 경쟁제품 규칙으로 강제하면 09-07(−0.105)의 재현이다.

**다음 방향은 B(조문 구현)가 아니라 C(그룹 재편)다.** dev200n 에서 문맥 간섭이
실측됐고(v16·v18 문구 변경이 손대지 않은 v10·v13 을 흔들었다), G2 정밀도 0.434 가
G1 0.725 와 크게 벌어져 있다. 다만 간섭의 **방향을 예측할 수 없어** API 1회가 필요하다.

## 🔬 09-09 대개선 — 판정 아키텍처 교체

미세조정을 멈추고 출력 구조 자체를 바꿨다. 후보 두 개를 dev 40건으로 먼저 걸렀다.

| 아키텍처 | dev **전체 200건** Macro | TP | FP | FN | 판정 |
|---|---|---|---|---|---|
| dev200n 기준선(등급형·문턱 2) | 0.6850 | 127 | 121 | 26 | — |
| **A 선택형** — 위반 항목만 배열로 고른다 | (40건에서 FP +12) | — | — | — | ⛔ 기각 |
| **B 양면판단** — `적법근거`(≤80자) → 등급 순서 = dev200r | **0.6609** | 121 | 126 | 32 | ⛔ 기각 |
| **문턱 3** (같은 등급 파일, API 0회) | **0.6992** | 125 | 105 | 28 | ✅ **채택** |

### ⚠️ 40건 예선은 쓰지 말 것 — 이번에 크게 속았다
```
40건 예선   기준선 0.7868 → 양면판단 0.8455   (+0.0587, 채택으로 판단)
200건 전체  기준선 0.6850 → 양면판단 0.6609   (−0.0241, 기각)
같은 40건을 전체와 같은 방식으로 재계산하면 0.6579 → 0.6064 (−0.0515)
```
40건에서는 항목당 정답 양성이 1~2건이라 F1 이 계단식으로 튄다. 부호까지 뒤집힌다.
**예비 측정도 200건으로 한다.** 절반만 돌려 시간을 버는 거래는 성립하지 않는다.

**A 가 실패한 이유 (측정으로 확정)**
등급 문턱 2 가 사실상 FP 필터였다. 선택형은 중간 등급을 없애(분포 `{0:638, 3:69}`)
등급 1 로 눌려 있던 약한 의심을 전부 양성으로 승격시킨다. "위반 없음을 24번의 부정이
아니라 한 번의 빈 배열로" 라는 착상은 맞았지만 잃는 것이 더 크다.

**B 가 실패한 이유 (200건 측정)**
항목별로 보면 v17 +0.179 · v18 +0.100 · v13 +0.075 는 실제로 개선됐다 — 반증을 먼저
쓰게 하는 것이 **부재탐지·병기 판단에는** 통했다. 그러나 v9 −0.200 · v11 −0.150 ·
v15 −0.123 · v2 −0.093 에서 더 잃었다. 적법근거를 찾아낸 뒤 그것을 근거로 **적법하다고
확신해 버리는** 반대 방향 오류가 생긴다(v15 는 TP 6→4, FN 0→2). 순합이 음수다.

**아래는 40건 예선 당시의 (틀린) 해석 — 기록으로만 남긴다**
스키마의 필드 순서가 곧 생성 순서다. `적법근거` 를 등급 **앞에** 두면 등급을 정하기 전에
"이 공고가 지켰다고 볼 근거"를 한 번 탐색하게 된다. 위반만 찾으라는 압력이 만들던
공고당 0.6건의 오탐이 여기서 깎인다. **프롬프트 지시로는 안 바뀌던 것이 출력 순서로 바뀌었다** —
지금까지 프롬프트 5/5 실패의 원인이 '지시'였고, 이번에 바꾼 것은 '구조'다.

**길이를 줄였더니 더 좋아졌다 (0.8319 → 0.8455)**
300자를 주면 모델이 근거를 길게 쓰며 자기 논증에 끌려간다. 80자면 핵심만 적는다.
게다가 출력량이 줄어 시간예산도 산다.

**시간예산 — 아키텍처와 별개로 남는 실측치**
```
캐시 실측 출력 중앙값   기존 154자 → 300자판 594자(2.77배) → 80자판 347자(2.25배)
평가셋 환산            1,853건 × 4회 = 7,412 호출 / 6,300초 = 초당 1.18회 필요
```
초과분은 **0 으로 제출**되어 재현율을 직접 깎는다. 그래서 두 가지를 넣었다.
1. 적법근거 80자 상한 (출력 2.77배 → 2.25배)
2. **시간압박 폴백** — 남은 시간이 부족하면 보강 호출을 버리는 대신 **양면 판단만 끄고
   계속한다**(`pps/pipeline.py` `_bundle`). 버린 칸은 0 이지만 짧은 판정이라도 받으면
   그 칸의 재현율이 살아난다.

**부재탐지 항목에만 양면 판단을 쓰는 절충안도 기각** (문턱 3 기준, API 0회 분해)
```
부재탐지 5개(v10 v11 v16 v18 v20) 평균 F1   0.5459 → 0.5455  (-0.0004)
나머지 19개                     평균 F1   0.7364 → 0.7263  (-0.0101)
조합(부재=양면 · 나머지=기준선) Macro 0.6966 vs 기준선 0.6967 → 차이 없음
```
"반증을 먼저 쓰는 것이 부재탐지에 통한다"는 40건 해석은 항목 단위로도 성립하지 않는다.
개선된 v17·v13·v14 는 부재탐지가 **아니고**, 부재탐지인 v11 은 −0.179 로 가장 크게 나빠졌다.
양면 판단은 어느 부분집합으로도 살릴 수 없다. **완전 기각.**

### ✅ 문턱 3 — 오늘의 실질 개선 (API 0회)

저장된 등급 파일로 스윕했다. **세 독립 실행 전부에서 3 이 이겼다.**
```
              문턱 2      문턱 3
dev200n      0.6850  →   0.6992   (+0.0142)   FP 121→105 · TP 127→125
dev200m      0.6788  →   0.6923   (+0.0135)
dev200o      0.6708  →   0.6834   (+0.0126)
```
**이유**: 등급 2 는 19칸(0.5%)뿐인데 그중 **17개가 FP, 2개만 TP — 89% 오탐**이다.
모델이 '확실하지 않다'고 표시한 칸은 실제로 대부분 틀린다. 등급 신호는 미약하지만 실재한다.
실제 분포는 dev 보다 정밀도가 낮으므로(0.331 vs 0.508) LB 에서 이 거래가 더 유리하다.
문턱 1 과 2 는 결과가 완전히 같다 — 등급 1 을 쓴 칸이 0개다.

**이것이 threshold sweep 구조를 만든 이유 그대로다.** 800회 호출을 다시 하지 않고
저장된 등급만으로 +0.0142 를 얻었다. `GRADE_THRESHOLD_DEFAULT = 3` 로 반영했다.

---

## 🔬 09-10 FP 전수 분석 · 적용성 게이트 (dev200s = 0.7162)

### 무엇을 바꿨나
```
① 소액수의 × v10~v13 게이트   Macro +0.0091  FP −9 · TP −0   (결정론적, API 0회 검증)
② 금액대조 블록 결론 어휘 제거   Macro +0.0079  v24 FP 14→10   (프롬프트, dev200s 로 검증)
                              ─────────────
   dev200n 0.6992 → dev200s   +0.0170 = **0.7162**   TP 125→127 · FP 105→94 · FN 28→26
```

### FP 105건 전수 분석에서 나온 것

**(1) FP 는 공고 내용과 무관한 고정 오탐률이다**
```
완전 적법 공고 112건 → FP 60건 = 공고당 0.54
위반 있는 공고  88건 → FP 45건 = 공고당 0.51     비 0.95
```
"한 번 찾으면 더 찾는" 연쇄가 아니다. 항목마다 독립적으로 걸린다.
v14~v18 은 **동시 FP 0건** — 금액 밴드 gating 이 배타성을 이미 완전히 지킨다.
따라서 **항목 독립 판정 구조 자체는 FP 의 원인이 아니다.** 공고-level 연쇄 가설은 기각.

**(2) FP 의 85%가 '읽기'가 아니라 '적용'의 실패다**
```
A 문서 오독(환각·무관 문구)  15건 14%
B 적용범위 오판             89건 85%
대조군: TP 인용형 94% 가 원문에 존재 · FP 인용형 84%  → 읽기 정확도로는 구별 불가
```
기업규모 4항목은 FP 와 TP 가 **100% 같은 문구**를 근거로 든다(v13 11/11, v17 9/9,
v14 3/3, v15 6/6). 부재탐지 5항목도 FP·TP 모두 해당 문구가 원문에 없다.
**문구를 더 잘 찾게 하는 개선은 원리적으로 효과가 없다.**

**(3) 적용성은 LLM 에게 물으면 안 된다 — 3전 3패**
```
선택형(후보 생성)      40건 FP +12
양면판단(적용 선탐색)   200건 −0.0241
사실추출 2단계(Arch A)  12건 FP ±0 · TP −3   (추출이 실적 배수·과업 규격을 잃는다)
```
반대로 **결정론적 적용성은 매번 이겼다** — 금액 밴드 gating(동시 FP 0), 소액수의 게이트(FP −9·TP −0).
원칙: **적용성은 메타데이터·조문으로 결정하고, LLM 에게는 사실 확인만 시킨다.**

**(4) 우리가 만든 FP 가 있었다**
금액대조 블록의 "본문에 동일 금액 없음"을 근거로 든 FP 5건, 같은 문구를 쓴 TP 0건.
배정예산이 **1원·2원·6원**인 데이터 이상 공고까지 "불일치"라고 알려 주고 있었다.
→ 1,000원 미만이면 대조를 접고, "없음"(결론) 대신 "찾지 못함 + 검색 범위 밖일 수 있음"(사실)으로 바꿨다.
(지역 마커 `[지역:r1|…]` 는 원본 데이터의 비식별 토큰이다 — 123/200 공고에 있다. 우리 것이 아니다.)

**(5) 조문 근거 있는 적용성 축은 거의 고갈됐다**
항목 × 메타축 교차표에서 '예측 양성 ≥3 · 정답 양성 0'인 칸이 12개 나왔지만,
조문 근거가 있는 것은 **소액수의 × v10~v13 하나뿐**이었다.
나머지(v9×일반용역, v10×물품 등)는 dev 상관이며, gating.py·gosimatch.py 에
"조문 근거 없음"으로 이미 기록돼 있다. 2026-09-07 실패(LB −0.105)의 축과 같은 성질이다.

### 회귀 검사
```
✅ v21 1.000→1.000 · v23 0.833→0.833 · v3 1.000→1.000 · v5 1.000→1.000 · v6 0.923→0.923
⚠️ v7  0.933→0.857  TP 7→6
```
v7 손실 1건(PPS-DEV-09)은 등급이 3→2 로 **한 칸 내려간 경계 변동**이지 체계적 파괴가 아니다.
같은 실행에서 TP 를 4건 얻고 2건 잃어 순 +2 다. 문턱 2 로 낮추면 살아나지만
그때 Macro 는 0.7130 으로 오히려 낮다.

---

## 📌 09-09 마감 상태 · 내일 착수점

**오늘 제출 (21:48) — 잘못된 파일이 나갔다**
21:37 에 submit.zip 을 양면판단 ON 으로 미리 빌드해 둔 것이 그대로 제출됐다.
40건 예선(+0.0587)만 보고 기본값을 켠 것이 원인이고, 200건 전체 측정은 그 뒤에 끝났다.
그 설정은 dev 0.6609 (기준선 0.6850 대비 −0.0241) 이므로 **LB 하락을 예상한다.**
→ 교훈: **측정이 끝나기 전에 제출 기본값을 바꾸지 않는다.**

**현재 submit.zip (22:27 재빌드) — 내일 제출용, 준비 완료**
```
양면판단 OFF · 등급모드 · 문턱 3 · max_tokens 1200
dev200n 등급 파일 기준 Macro 0.6992  (기존 best 0.6850, +0.0142 · API 0회)
스모크 PASS · 게이팅 감사 통과 · test_threshold PASS · 금지어 검사 통과
```

**내일 우선순위**
1. 위 파일을 먼저 제출해 문턱 3 의 LB 효과를 측정한다 — API 0회로 얻은 개선이므로
   확인 비용이 없다. 전이비율 0.70 을 적용하면 LB 0.49 근처가 기대값이다.
2. 남은 구조 후보는 전부 소진됐다(프롬프트 5/5, 선택형, 양면판단, 그룹분할, 합의,
   notice-level 게이트, 결정규칙 탐색). **다음 실험은 반드시 200건 전체로 측정한다.**
3. 아직 안 해 본 것: 등급 척도를 0~5 로 넓혀 **등급 2가 0.5% 뿐인 문제**를 푸는 것.
   문턱 3 의 이득이 '등급 2 는 89% 오탐'에서 나왔으므로, 중간 등급을 더 잘게 쓰게 하면
   버릴 칸을 더 정확히 고를 수 있다. 유일하게 근거가 있는 미시도 항목이다.

---

## 🔬 09-10 선택적 dual (v13~v18) — 기각

조문으로 정의된 항목군(판로지원법 시행령 제2조의2·제2조의3 + 국가령21/지방령20)에만
양면 판단을 적용했다. 오프라인 추정 +0.0124 였지만 **실측은 +0.0017** 이다.

```
① baseline dev200s        TP 127 FP 94 FN 26  0.7162
② 선택 dual (v13~v18)      TP 123 FP 86 FN 30  0.7179  +0.0017
③ ① + v24 교집합           TP 125 FP 84 FN 28  0.7177  +0.0015
④ ② + v24 교집합           TP 121 FP 77 FN 32  0.7187  +0.0024
```

**기각 사유 — 강한 항목을 파괴한다**
```
v13 +0.099 · v14 +0.087 · v16 +0.078 · v17 +0.017   (FP −9)
v15 −0.200 ← TP 6→4.  baseline 에서 F1 1.000 이던 항목이다.
v18 −0.026
```
FP 는 확실히 줄지만 **F1 1.000 이던 v15 를 0.800 으로 깎는다.** 순증 +0.0017 은
노이즈 수준이고, "Macro 가 조금 올라도 강한 항목을 크게 파괴하면 채택하지 않는다"는
원칙에 걸린다. v15 만 대상에서 빼면 오르지만 그러면 '같은 조문을 공유하는 항목군'이라는
정의 근거가 깨지고 dev 점수로 항목을 고른 것이 된다.

**측정 방법의 구조적 한계도 확인했다**
dual 모드는 max_tokens 를 1200→1800 으로 올린다. 캐시 키에 max_tokens 가 들어가므로
**프롬프트가 바이트 동일한 그룹(G1·G4)까지 전부 재호출**되고 미세 변동이 생긴다
(v1 +0.127 이 그 예다. 결손 0 · 프롬프트 지문 동일인데 값이 달라졌다).
같은 그룹 안의 비대상 항목은 문맥 간섭으로 더 크게 흔들린다(v11 −0.124).
→ **max_tokens 를 바꾸는 실험은 '한 변수만 바꾼 비교'가 원리적으로 불가능하다.**

**부수 확인**: 결손 28칸이 있던 1차 실행과 결손 0 인 재실행의 Macro·등급분포가
완전히 같았다(0.7179 · `{0:3119, 2:17, 3:211}`). 즉 그 칸들은 결손이 아니라
실제 0 판정이었다. "결손이 채워지면 v15 가 회복될 것"이라는 내 예측은 틀렸다.

**결론: dev200s 0.7162 를 유지한다.**

---

## ⛔ 실패한 접근 — 반복하지 말 것

| 시도 | 결과 | 왜 안 됐나 |
|---|---|---|
| **선택형 출력**(위반 항목만 배열로 고르기, `--select`) | 40건 TP +1 / **FP +12** (16→28) | 등급 문턱 2 가 실제로는 FP 필터였다. 선택형은 중간 등급을 없애(분포 `{0:638, 3:69}`) 등급 1 로 눌려 있던 약한 의심을 전부 양성으로 승격시킨다. **"위반 없음을 한 번의 빈 배열로"** 라는 착상은 맞았지만, 잃는 것이 더 크다 |
| 기저율 편향 프롬프트("기본값 0") | 예측 206→187, 상한 −0.004 | 지시로 모델 성향이 안 바뀐다 |
| 항목별 배제조건 명시 | dev −0.010 | 거짓양성과 함께 진짜양성도 줄었다 |
| **2단계 검증(재질의)** | dev −0.095, 상한 **−0.063** | 취소 80건 중 진짜양성 28건. 선별도 65%로 무작위(18%)보다는 낫지만, 같은 모델이 같은 오류를 반복한다 |
| 근거문구 없으면 억제 | dev −0.009 | 규칙 강제 판정(v23)이 근거가 없어 같이 죽는다 |
| 바이그램 품명 유사도 | 정밀도 0.14 | '통학차량 임차'→'교통신호등' |
| v14/v17/v18 물품 게이팅 | — | **항목명의 "일반물품"은 업무구분이 아니다.** v18 양성 7건 중 5건이 일반용역. 조문(영 21①10호)이 "물품 또는 용역"이라 쓴다 |
| **세부품명 미등록 게이팅 (v10·v11·v13)** | **리더보드 −0.105** | dev 상관관계를 조문으로 착각했다. 09-07 제출로 반증 |
| **v9 `is_물품` 게이팅** | 위와 같은 제출에 포함 | 위의 "일반물품 ≠ 업무구분" 실수를 반복 |
| `_rule_hints` v10·v11 강제 0 | dev 양성 6건 억제 | '직접생산'이 계약조건 상투문구로 200건 중 133건에 등장 |

**공통 패턴 — 실패한 9가지가 전부 억제(예측 줄이기)다.**
성공한 변경은 둘 다 **예측을 만들어낸** 조문 구현이었다.
```
v23  LLM F1 0.000 → 규칙 F1 0.909   (설명회 일정 검산)
v21  LLM F1 0.500 → 규칙 F1 1.000   (공동수급 최소지분율)
```
09-07 제출이 이걸 리더보드에서 확인해줬다: 억제 = −0.105.
→ **앞으로 억제는 리더보드로 검증되기 전에는 넣지 않는다.**
→ 조문 구현은 2회 시도 2회 성공, 프롬프트 엔지니어링은 5회 시도 5회 실패다.

**조문 구현이 통하는 항목의 조건** (v21·v23 공통)
1. 조문이 **숫자 기준**을 준다 (일수, 백분율, 금액)
2. 공고문에 그 숫자가 **정형적으로 표기**된다
3. 코드가 두 값을 견주기만 하면 판정이 끝난다 — **의미 판단이 없다**

### ⛔ 규칙 후보 9개를 실측했고 전부 LLM 보다 나빴다 (2026-09-07)

여력 계산상 최대 항목도 +0.034 뿐이라 여러 항목을 잡아야 했다. 가장 큰 일관 블록인
기업규모 계열(v14~v18, 합계 +0.053)부터 쳤는데 전멸했다. **다시 시도하지 말 것.**

| 항목 | 시도한 규칙 | 규칙 F1 | LLM F1 |
|---|---|---|---|
| v14 | 밴드C + (중소\|소기업) 제한 | 0.485 (FP 17) | 0.750 |
| v15 | 밴드B + 소기업만 | 0.000 | 0.833 |
| v16 | 밴드B + 제한 없음 | 0.444 (FP 8) | 0.589 |
| v17 | 밴드A + 중소기업 제한 | 0.116 (**FP 75**) | 0.400 |
| v18 | 밴드A + 소기업 제한 없음 | 0.343 (FP 22) | 0.556 |
| v24 | 등록금액이 본문에 없음 | 0.095 | 0.182 |
| v19 | 확약서 + 입찰단계 제출 문구 | 0.000 | 0.500 |
| v5 | 지역기준 이상 + 소재지 제한 표기 | 0.417 | 0.833 |
| v7 | 지역기준 미만 + 제한지역 2개 이상 | 0.400 | 0.666 |

**왜 실패했나 — 조건 3 을 어겼다.** 금액 밴드는 정형적이지만
**"제한을 실제로 했는가"는 의미 판단**이다. `restricted_to` 정규식은 참가자격 제한이
아닌 언급까지 다 잡는다(v17 거짓양성 75건). 원칙 3 그대로다.

v21·v23 이 통한 건 **본문에 명시적 숫자 하나가 있고 조문 임계값 하나와 대조하면 끝**나서다.
지분율 `3%`, 설명회~마감 `일수` — 둘 다 의미 판단이 0 이다.
→ **남은 항목에 이 형태가 더 있는지 회의적이다.** 규칙 경로는 사실상 소진됐다고 본다.

**부산물**: 이 규칙들은 재현율이 높고 정밀도만 낮다(v14 R=1.000 · v18 R=0.857 ·
v17 R=0.833). **판정자로는 못 쓰지만 후보 생성기로는 쓸 수 있다** —
"이 공고는 밴드A 인데 중소기업 표기가 있다"를 사실로 주는 식. 아직 시도 안 했다.

### ⛔ v24 규칙 — 만들었고 LLM 보다 나은데 **배선하지 않았다** (2026-09-07)

v24 는 최악 항목(F1 0.182 · 여력 +0.034)이고 '값 대조'라 규칙에 가장 잘 맞아 보였다.
정답 8건 중 근거문구가 있는 건 1건뿐이어서(v21 과 같은 패턴) 원문을 직접 읽어
불일치의 실제 형태를 규명했다.

```
PPS-DEV-29  본문 "추정가격 34,481,818원"       vs 등록 36,118,183원      ← 금액 불일치
PPS-DEV-057 본문 "입찰방법 : 제한경쟁입찰(…)"  vs 등록 계약방법=일반경쟁  ← 경쟁유형 불일치
나머지 6건  레이블 붙은 금액·경쟁유형 불일치가 없다 (다른 필드이거나 표기가 없다)
```

`compare.check_v24_positive()` 로 두 형태를 구현했다. 결과:

| | TP | FP | FN | P | R | F1 |
|---|---|---|---|---|---|---|
| LLM 단독 | 2 | 12 | 6 | 0.143 | 0.250 | 0.182 |
| **규칙 단독** | 2 | 2 | 6 | 0.500 | 0.250 | **0.333** |
| 합집합(양성 전용 힌트) | 2 | 15 | 6 | 0.118 | 0.250 | **0.160** |

**규칙이 LLM 보다 나은데도 합집합은 LLM 보다 나쁘다** — 규칙이 맞춘 2건이
LLM 이 이미 맞춘 것과 **같은 레코드**라 새 TP 는 0 이고 FP 만 늘어난다.
"양성 전용이면 잃을 게 없다"는 직관이 여기서 깨졌다. 정밀도는 잃는다.

규칙 단독으로 넘기면 dev 는 +0.151 오르지만 **양성 8건 표본**이다 —
09-07 세부품명 게이팅(양성 19건 · dev 상승 · 리더보드 −0.105)과 같은 도박이다.
→ 함수는 측정 기록으로 남기고 배선하지 않았다. 리더보드 여력이 생기면 시험할 만하다.

**곁가지로 잡은 실제 버그** — `compare.amounts_in` 의 자릿수 단위.
"사업예산: 1,250,000천원"(=12.5억)을 1,250,000원으로 읽어 **1,000배 틀렸다.**
`amount_is_matching_quote`(v24 억제)와 `amount_check`(프롬프트 사실)가 이걸 쓴다.
`_AMT_SUFFIX` 를 먼저 처리하고 그 구간을 제외하도록 고쳤다. 단위 테스트 6/6 통과.

**성공 2건 / 실패 다수**
```
조문 구현       2회 시도 2회 성공  (v23 0.909 · v21 1.000)
프롬프트 엔지니어링 5회 시도 5회 실패
규칙 후보(위 표)  9회 시도 9회 실패
```

### 고친 실제 버그
- `소기업` 정규식이 **`중소기업기본법`의 부분문자열에 걸렸다.** dev 200건 중 107건이
  "소기업 제한 있음"으로 오판되어 v18 의 진짜 양성을 규칙이 억제했다.
  → 부정 후방탐색으로 수정. v18 0.476→0.588, v17 0.200→0.364 (예측 감소, 재현율 유지)
- `calibrate.py` 가 재현율 0 을 1.0 으로 갈음해 상한을 부풀렸다.

## 측정 이력

| 라운드 | 대상 | Macro F1 | 변경 |
|---|---|---|---|
| dev20 | 20건/16항목 | 0.6125 | 첫 측정 |
| dev40a | 40건 | 0.6232 | 오답 6종 수정 |
| dev40b | 40건 | 0.6732 | 조항호 오염 제거 |
| **dev200** | **200건** | **0.6282** | v23 규칙 + 지역 단서 |
| **dev200b** | 200건 | **0.6326** | gosimatch + v9 물품게이팅 (+0.004, 사실상 무변화) |
| dev200c | 200건 | 측정 중 | 기본값0 편향 + 배제조건 |
| dev200c/d/e/f | 200건 | — | 기본값0 편향 · 배제조건 · 2단계 검증 (전부 실패, 위 표) |
| **dev200g** | 200건 | **0.6455** | 세부품명 게이팅 ON → **제출해서 0.31943 (실패)** |
| **dev200h** | 200건 | **0.6187** | 게이팅 되돌림. ⚠️ API 장애로 **빈 출력 58건** 오염 — v20 이 0.727→0.286 으로 붕괴한 건 코드 탓이 아니다. 재측정 필요 |
| holdout | 200건 | — | 라벨 없음. 예측률만 잼 |

**제출 2회**: 09-06 → **0.42458** · 09-07 → **0.31943**

### dev 는 리더보드 방향조차 못 맞힌다 (실측)
dev200b(0.6326) → dev200g(0.6455) 는 **+0.013**,
같은 두 코드의 리더보드는 0.42458 → 0.31943 으로 **−0.105**. 부호가 반대다.
**dev 로 제출 여부를 결정하면 안 된다.**
dev 로 볼 수 있는 것은 "이 변경이 dev 진짜 양성을 죽이는가"(재현율) 하나뿐이고,
그것도 항목당 양성 5~8건이라 15% 이하 손실은 안 보인다.

### 소규모 측정은 믿지 말 것
항목당 양성이 6~8건뿐이라 200건에서도 항목별 F1 이 ±0.15 흔들린다.
dev40 에서 v9 이 0.000→1.000 이라 성공으로 봤는데 dev200 에서는 0.300 이었다(예측 14/정답 6).
**항목 하나씩 붙잡는 방식은 수렴하지 않는다. 전 항목에 걸리는 체계적 변경만 신뢰할 것.**

---

## 구조

```
pps/
  law.py        조문 임계값 (고시금액·판로지원 밴드·실적배수·SW하한) — 전부 법령패키지 근거
  gating.py     항목별 적용조건 — 금액밴드·계약법 등 조문이 적용대상을 한정하는 것만 남김
                (세부품명·물품구분 게이트는 09-07 제출로 반증되어 끔. 함수는 경고와 함께 보존)
  sections.py   관련 절만 압축 (3,000자에서 근거 포함률 90.7%, 베이스라인 79.6%)
  presence.py   부재탐지 전체문서 스캔 (절단 없음)
  gosimatch.py  중기부고시 경쟁제품 대조 — 용역 카탈로그 29개를 프롬프트에 직접 제공
  compare.py    공고문↔나라장터 값 대조, 조항호 해석, 공동수급 지분율 하한 계산(v21)
  schedule.py   설명회 일정 검산 (v23, 규칙 F1 0.909)
  spec.py       규격서 모델명 후보 추출 (v9)
  prompts.py    4개 그룹 프롬프트 + 2단계 검증 프롬프트
  pipeline.py   게이팅 → 그룹 판정 → 2단계 검증 → 규칙 결합 → 근거 정합
  runner.py     Mock / API(개발전용, 제출시 제거) / vLLM
tools/
  evaluate.py   dev 채점        audit_gates.py  게이팅 안전성
  calibrate.py  예측률 상한     analyze_errors.py 오답 원인 분류
  probe_*.py    섹션·부재탐지 검증, probe_v21.py 공동수급 지분율 원문 조사(LLM 호출 없음)
build_submit.py  submit.zip 생성 (외부 API 코드 제거 검증 포함)
```

### 확립한 원칙
1. **규칙은 LLM 보다 확실히 나을 때만 덮어쓴다.** 아니면 사실로만 제공한다.
   v23: 규칙 F1 0.909 vs LLM 0.000 → 강제.
   v6: 규칙 F1 0.32 → 단서로만. v21: 규칙이 양성 6건 중 1건만 설명 → 사실로만.
2. **게이팅은 조문이 적용 대상을 한정할 때만 켠다.** 메타 필드의 존재 여부는 조문이 아니다.
   dev 에서 "죽인 양성 0건"은 필요조건일 뿐이다 (양성 6~8건 → 15% 손실은 안 보인다).
3. **정규식으로 의미 매칭을 흉내내지 않는다.** 후보만 좁히고 판단은 LLM 에 맡긴다.
   (바이그램 매칭이 '통학차량 임차'를 '교통신호등'에 붙였다. 정밀도 0.14)
4. **부재탐지는 정규식이 낫다** — 단 **문구가 정형적일 때만**.
   v20(대기업 참여제한 명시)은 정형적이라 맞았고, v10 의 '직접생산'은
   계약조건 상투문구로 200건 중 133건에 등장해 틀렸다.
5. **억제 변경은 리더보드로만 검증한다.** dev 도 `calibrate.py` 상한도 억제를 항상
   좋게 평가한다(상한은 예측률이 분모라 기계적으로 오른다). 09-07 제출이 이걸 증명했다.

---

## 다음 할 일

**전제: 우리는 재현율이 병목이다.** 억제 9종이 전부 실패했고 09-07 제출이 −0.105 였다.
놓침의 82% 가 "그룹 전체 침묵"이다. 아래는 전부 **예측을 만들어내는** 작업이다.

1. **09-08 제출** — 위 §"09-08 제출 후보"를 그대로 올린다. 0.42458 과 비교해
   "억제 제거"의 순효과를 단독으로 측정한다. 오르면 방향 확정, 내리면 최적점을 앞뒤로 좁힌 것.
2. **v1 놓침 4건 원문 조사** (PPS-DEV-046, 058, 063, 072) — API 없이 가능.
   `tools/probe_v21.py` 를 본떠 만들면 된다. v21 에서 "근거 빈칸 = 못 잡는다"가
   **반증됐으므로**(§정정) v1 도 원문에 근거가 있을 가능성이 높다.
   정밀도 1.00 · 재현율 0.43 이라 잡는 만큼 순이득이다.
3. **조문 구현 후보 찾기** — v21·v23 이 통한 조건은 "조문이 숫자 기준을 주고,
   공고문에 정형적으로 표기되고, 두 값 대조로 판정이 끝난다"였다. 2/2 성공.
   `v3`(실적 1배수), `v14~v18`(금액 밴드), `v7`(지역 범위)이 이 형태에 가깝다.
4. **판정 문턱 낮추기** — 놓침 42/51 이 그룹 전체 침묵이다. 항목별 손질로는 안 된다.
   후보: 스키마를 `위반여부 0|1` 에서 등급 `0~3` 으로 바꿔 **한 번의 측정으로 문턱을 스윕**한다
   (사후 임계값 조정이라 재측정 없이 ROC 를 얻는다). 지시문으로 성향을 바꾸는 건 이미 실패했다.
   ※ 단 `max_tokens` 수정으로 '침묵'의 일부가 절단이었을 수 있다 — 재측정 후 다시 판단할 것.
5. **dev200 재측정** — dev200h 는 빈 출력 58건으로 오염됐다. API 안정될 때 다시 돌린다.
   보는 값은 Macro F1 이 아니라 **항목별 재현율**과 **결손 항목 수**(392 → 얼마?)다.
6. **재현율 최약체** (dev200g 기준, v20·v21 제외): v24 0.25 · v17 0.33 ·
   v1 0.43 · v10 0.43 · v19 0.50 · v12 0.50 · v7 0.57.
7. **자가 라벨링** (규칙 2-2 허용) — holdout200 에 라벨을 붙여 **실제 분포의 검증셋**을
   만든다. dev 가 인위적 균형 표본이라 이것 없이는 오프라인 신호가 계속 틀린다.

## 🔧 문턱 스윕 구조 (2026-09-08 구현)

**API 800건 한 번 → 원본 등급 저장 → API 없이 문턱 스윕.**

왜: 판정을 이진 0/1 로 받으면 문턱이 모델 안에 숨는다. 문턱 하나를 바꿔 보려면
dev 200×4 = API 800건을 다시 호출해야 하고, 그 API 는 85분이 걸리고 약 10%가 끊긴다.
등급(0~3)을 원본 그대로 저장하면 이후 비교는 전부 로컬이다.

```
Gemma → 위반등급 0~3 + 근거문구  →  [저장]  →  문턱  →  최종 0/1
        (LLM 원본, 변형 안 함)              (finalize)
```

| 구성요소 | 역할 |
|---|---|
| `prompts.build_schema(items, graded=True)` | `위반여부 0\|1` → `위반등급 0~3` 스키마 |
| `prompts.GRADE_GUIDE` | 등급 뜻(3 명백 / 2 위반으로 보임 / 1 의심 / 0 아님) |
| `pipeline.parse_group(.., graded)` | **최종 0/1 을 만들지 않는다.** 원본 등급만 보존 |
| `pipeline.GRADE_THRESHOLD_DEFAULT` | **문턱을 관리하는 단 하나의 값** (현재 2) |
| `pipeline.finalize(.., threshold=)` | 등급 → 0/1 로 바뀌는 **유일한 지점** |
| `pps/grades.py` | 원본 등급 저장·재로딩 (LLM 출력만 담는다) |
| `tools/sweep_threshold.py` | 저장물 재채점. **API 호출 없음** |
| `tools/test_threshold.py` | 구조 자체 검증 7종 |

**저장물은 LLM 출력만 담는다** — 게이팅·규칙·근거정합은 재채점 때 현재 코드로 다시 만든다.
그래서 규칙을 고친 뒤에도 **같은 저장물로 재채점해 규칙 효과를 견줄 수 있다.**

```bash
# 1) API 800건 — 딱 한 번
python3 tools/evaluate.py --runner api --graded --save-grades out/g.json

# 2) 이후는 전부 로컬 (네트워크 차단 상태에서도 완주함을 확인했다)
python3 tools/sweep_threshold.py out/g.json
python3 tools/sweep_threshold.py out/g.json --per-item
python3 tools/sweep_threshold.py out/g.json --threshold 2 --save-csv out/t2.csv
```

### 회귀 안전 — 이진 모드는 그대로다
`graded=False` 면 `1 → GRADE_MAX(3)`, `0 → 0` 으로 담는다. 따라서 **문턱 1~3 어디서나
기존 판정이 그대로 재현된다.** 제출본은 인자 없이 실행되므로 이진 모드 + 문턱 2 이고,
mock 평가가 변경 전과 동일하다(v21 1.000 · v23 0.909 · 게이팅 27.4% · Macro 0.0795).

⚠️ **구현 중 잡은 사고**: 처음엔 이진 1 을 등급 1 로 담았다. 그러면 기본 문턱 2 에서
**모든 이진 판정이 0 이 된다** — 제출본이 전부 0 이 되는 사고였다.
`tools/test_threshold.py` 의 파싱 검사가 잡았다. 값을 **어느 키에서 얻었는지**가
해석을 정한다(`위반여부`에서 온 1 은 등급 1 이 아니라 3).

### 검증 (전부 API 없음)
```
tools/test_threshold.py         7종 전체 PASS
  1 v24 회귀 · 2 등급 파싱 · 3 문턱 판정 · 4 스윕 단조성
  5 저장/재로딩 · 6 API 무호출 · 7 제출 형식
소켓 차단 상태에서 스윕 완주      종료코드 0
합성 등급으로 판별력 확인          문턱1 F1 0.490 / 문턱2 0.790 / 문턱3 0.700 → 2 선택
build_submit.py --smoke          자가검증 PASS · 판정모드 이진 · 문턱 2
제출본 zip                       금지어 5종 0건 · 모듈 16개(grades 포함)
```

### 📊 실측 결과 — dev200i (2026-09-08, API 800건 1회)

```
등급 분포 {0: 3220, 2: 19, 3: 248}     ← 등급 1 이 **0개**
빈 출력 0 · 결손 항목 7 · 소요 3400s · API 실패 0
```

| 문턱 | 예측+ | TP | FP | FN | 정밀도 | 재현율 | Macro F1 |
|---|---|---|---|---|---|---|---|
| 1 | 265 | 123 | 142 | 30 | 0.464 | 0.804 | **0.6480** |
| 2 | 265 | 123 | 142 | 30 | 0.464 | 0.804 | **0.6480** |
| 3 | 247 | 119 | 128 | 34 | 0.482 | 0.778 | 0.6451 |

**⛔ 문턱 스윕은 이 항목집합에서 지렛대가 아니다.**
- 등급 1 이 0개라 **문턱 1 과 2 는 산술적으로 동일**하다. 작동점이 3개가 아니라 2개다.
- 문턱 3 은 등급 2 인 19칸(예측의 7%)만 버린다 → 정밀도 +0.018, 재현율 −0.026,
  **Macro F1 −0.0029**. 사실상 무변화다.
- 모델은 0/3 으로만 답한다(3487칸 중 등급 2 가 19칸, 0.5%). 스케일을 줬는데 쓰지 않는다.

**얻은 것**: "판정 문턱을 낮춰 재현율을 사자"는 가설이 **반증됐다.**
모델이 확신 없는 칸을 중간 등급으로 표시하지 않으므로, 문턱을 조작해 꺼낼
숨은 재현율이 없다. 놓침 30건은 문턱 문제가 아니라 모델이 **0 이라고 확신한** 것이다.

### ✅ 결손 항목 392 → 7 (절단 재시도 수정이 통했다)
09-07 에 넣은 `runner._looks_truncated()` + 재시도의 효과다. API 실패 0건.
"결손은 토큰 상한이 아니라 연결 끊김"이라는 진단이 실측으로 확인됐다.
**dev 측정이 이제 신뢰할 만하다** — 이전 측정들은 그룹 호출의 ~10%를 잃은 값이었다.

### 항목별 문턱도 지렛대가 아니다
th3 이 나은 항목은 v3·v4·v19·v24 뿐이고, 전부 최적으로 골라도 0.6480 → 0.6578
(+0.0098)이다. 같은 200건에서 항목마다 최적을 고른 **과적합 상한**이라 실제로는 더 낮다.
항목당 양성 5~8건에서 ±0.15 흔들리는 값이므로 신뢰하지 말 것.

### 다음에 등급 모드를 다시 쓴다면
등급 1·2 가 안 나오는 게 원인이다. `GRADE_GUIDE` 가 "확신의 정도"라는 **주관적 기준**을
준 것이 문제로 보인다 — 모델은 감각보다 **관찰 가능한 조건**을 따른다. 예:
"근거 문구를 지목할 수 있으나 그 문구가 금액대·조건까지 충족하는지 확인 못 했으면 2".
다만 이번 실측이 보여준 건 **문턱 자체의 여력이 작다**는 것이므로 우선순위는 낮다.

## 🔎 dev200i FN 30건 분석 → 프롬프트·사실블록 수정 (2026-09-08, 미검증)

### 단계별 분포 — **파이프라인 결함 0건**
```
게이팅 차단 0 · 파싱/결손 0 · finalize(규칙이 되돌림) 0
증거 미전달 1 (v22 PPS-DEV-137)
LLM 판단 29        ← 근거는 갔는데 모델이 0 이라고 답한 것
```
형제 항목 혼동 9/30(30%), 나머지 21건은 그룹 전체 침묵.
어제 넣은 절단 재시도로 결손 392→7 이 되면서 파싱 경로도 깨끗해졌다.

### 고친 것 1 — v17 "중소기업 또는 소상공인" 병기 (FN 5건)
DEV-21·035·042·053·073 **전부 밴드 A 이고 본문에 근거가 명확히 있다.**
정답 근거문구가 빈칸이었을 뿐이다(v21 에서 이미 반증한 그 패턴).

원인은 **프롬프트의 비대칭**이었다. G2 지시는 v13 에 대해 "'중소기업 또는 소상공인'처럼
중기업이 포함되면 v13 이 아니다"라고 명시하는데, **밴드 A 에서 같은 병기가 곧 v17**
이라는 역방향이 없었다. 모델은 "소상공인이 들어있으니 적법"으로 읽는다.
→ `prompts.py` G2 지시의 v17 줄에 병기 기준 추가. **v13 문구는 그대로 두고**
   "구간이 다르면 결론이 반대다 — v13 은 경쟁제품, 여기는 일반 물품·용역 1억 미만"을 붙여 충돌 방지.

### 고친 것 2 — `spec.equivalence_note` 가 판정을 대신했다 (FN 1건+)
"대체 허용 문구 있음" 이라고 **단정**했다. 프롬프트의 "동등 이상 문구가 있으면 위반이 아니다"와
맞물려 사실 블록이 결론을 내려 버린다. DEV-050 은 모델명(SH-F800·TSQ80·AGRAS T25 …)을
정확히 뽑아 놓고 이 한 줄 때문에 0 이 됐다.
→ "대체 관련 문구(원문 인용): …" + "이 문구가 **나열된 품목 전부에** 적용되는지 직접 확인하라".

### 고친 것 3 — `gosimatch` 의 "(확실한 근거)" (FN 2건+)
코드가 고시에 있다는 사실이 곧 "경쟁제품이니 직생 요구는 적법"으로 읽혀 v12 를 막았다.
```
DEV-053 조달대상 '알츠하이머 후보소재 비교·분석' vs 코드 '행사기획/전시회기획'
DEV-056 조달대상 '해운부문 감축사업 활성화 용역'  vs 코드 '인터넷지원개발서비스'
```
발주기관이 **무관한 코드를 붙여 직생확인을 요구한 것**이 v12 위반인데 블록이 반대로 안내했다.
→ "문서에 적혀 있는 고시 세부품명번호:" + **양방향 대칭** 지시
  ("서로 맞으면 경쟁제품 입찰이다 / 무관하면 경쟁제품 근거가 못 된다").
  v10·v11·v13 을 죽이지 않으려고 어느 쪽으로도 기울이지 않았다.

### ⚠️ 회귀 노출 (측정함)
`gosimatch` 코드 블록은 **50/200건**에만 붙는다. 그 안의 기존 정답:
```
v11 TP 3 · v13 TP 3 · v10 TP 0 · v12 TP 0   → 되돌릴 수 있는 TP 상한 6건
같은 범위의 기존 FP: v13 9 · v10 5 · v11 1  → 줄일 수 있는 FP 상한 15건
```
FP 노출이 TP 노출보다 크므로 기대값은 나쁘지 않지만 **보장은 없다.**

### 검증 (API 없음)
```
import/문법            OK
tools/test_threshold.py 7종 전체 PASS
tools/audit_gates.py    게이팅이 죽인 진짜 양성 0건
mock 회귀               v21 1.000 · v23 0.909 · 게이팅 27.4% · Macro 0.0795 (변경 전과 동일)
build_submit --smoke    자가검증 PASS · 49열
저장 등급 재채점         0.6480 → 0.6480 **완전 동일** = 판정 레이어 미변경 증명
프롬프트 길이            중앙 6,651자 · 최대 9,311자 (예산 여유 충분)
```

### ✅ 검증 완료 — dev200j (2026-09-08, API 800건 1회)

동일 조건(등급 모드·같은 시드·같은 문턱)에서 **프롬프트 세 곳만** 바꿔 재측정했다.

| | TP | FP | FN | 정밀도 | 재현율 | Macro F1 |
|---|---|---|---|---|---|---|
| dev200i (전) | 123 | 142 | 30 | 0.464 | 0.804 | 0.6480 |
| **dev200j (후)** | **129** | 143 | **24** | 0.474 | **0.843** | **0.6674** |
| 변화 | +6 | +1 | −6 | +0.010 | +0.039 | **+0.0195** |

**FP 를 +1 만 내주고 FN 을 6건 줄였다.** 억제 없이 재현율을 산 첫 사례다.

| 항목 | 전 F1 | 후 F1 | Δ | TP | FP | FN |
|---|---|---|---|---|---|---|
| **v17** | 0.182 | **0.571** | **+0.390** | 1→6 | 4→9 | **5→0** |
| v12 | 0.600 | 0.727 | +0.127 | 3→4 | 1→1 | 3→2 |
| v11 | 0.500 | 0.571 | +0.071 | 6→6 | **12→9** | 0→0 |
| v9 | 0.286 | 0.348 | +0.062 | 3→4 | 12→13 | 3→2 |
| v13 | 0.348 | 0.400 | +0.052 | 4→4 | **13→10** | 2→2 |
| v10 | 0.400 | 0.444 | +0.044 | 6→6 | **17→14** | 1→1 |
| v15 | 0.923 | 0.800 | −0.123 | 6→6 | 1→3 | 0→0 |
| v16 | 0.526 | 0.444 | −0.082 | 5→4 | 8→8 | 1→2 |

**v17 은 FN 5건이 전부 회복돼 재현율 1.000 이 됐다** — 병기 기준이 정확히 그 5건을 잡았다.

**gosimatch 회귀는 없었고 오히려 반대였다.** 걱정했던 v10·v11·v13 은 TP 를 하나도 잃지 않고
**FP 만 각각 3건씩 줄었다**(합계 −9). "코드 존재 ≠ 적절한 코드" 를 물은 것이
경쟁제품 오판을 줄인 것이다. 대칭 문구로 쓴 것이 주효했다.

이전 FN 30건 중 **8건 회복**(v17 5 · v9 1 · v12 1 · v13 1), 22건은 여전히 0.
새 FP 13건 · 사라진 FP 12건 → 순증 +1.
**새 FN 2건**(v13 PPS-DEV-074 · v16 PPS-DEV-067) — 유일한 실질 회귀다.

**대가**: v15 가 −0.123(FP 1→3), v16 이 −0.082 로 나빠졌다.
v17 병기 기준이 인접 구간(1억~고시금액)으로 번진 것으로 보인다 —
밴드 B 에서 "중소기업 제한"은 적법인데 v15/v16 판정이 흔들렸다.
v17 의 +0.390 이 이를 크게 상회하므로 순이득이지만, 다음에 손볼 곳은 여기다.

### 🔧 v15/v16 회귀의 원인은 **지시문 누수**였다 (2026-09-08, 미검증)

`build_messages` 가 `group.지시` 를 **통째로** 실었다. G2 는 금액 밴드로
v14(C)·v15·v16(B)·v17·v18(A) 이 게이팅상 **상호배타**인데 지시문은 늘 다섯 개를 다 실었다.
```
밴드A 106건 → v17·v18 을 묻는데 v14·v15·v16 지시까지 받음
밴드B  49건 → v15·v16 을 묻는데 v14·v17·v18 지시까지 받음
밴드C  45건 → v14 만 묻는데 v15·v16·v17·v18 지시까지 받음
```
즉 밴드B 공고가 **v17 용 병기 기준을 읽고 v15/v16 에 적용**했다.
이 모듈 맨 위 원칙("게이팅에서 제외된 항목은 프롬프트와 스키마 양쪽에서 아예 뺀다")을
지시문만 어기고 있었다.

**고친 방식** — `Group.항목지시: Dict[str, str]` 를 추가하고 `Group.지침(items)` 가
**묻는 항목의 기준만** 이어 붙인다. 문장 한 줄을 더한 게 아니라 경계를 구조로 만들었다.
`항목지시` 가 비면 기존과 동일하게 동작하므로 G1·G3·G4 는 무변경이다.

같이 손본 것(순수 이동이 아님): v15·v16 기준에 **허용 범위가 중소기업자까지**임을 명시했다.
v15 에 "'중소기업 또는 소상공인' 처럼 중소기업이 포함된 제한은 적법이며 v15 가 아니다"를
넣어, 병기 표현에 대한 판단이 밴드마다 반대라는 점을 각 항목 안에서 스스로 완결시켰다.

### ❌ dev200k 검증 — scope 격리는 성공, **전체는 실패** (Macro 0.6674 → 0.6585)

| | TP | FP | FN | Macro F1 |
|---|---|---|---|---|
| dev200i (원본) | 123 | 142 | 30 | 0.6480 |
| **dev200j (best)** | 129 | 143 | 24 | **0.6674** |
| dev200k (scope 수정) | 125 | 140 | 28 | 0.6585 |

**의도한 것은 정확히 됐다** — v15 0.800 → **0.923**(dev200i 수준 완전 회복),
v16 0.444 → **0.526**(완전 회복). 누수 차단이 목표한 두 항목을 그대로 되돌렸다.

**그런데 v17 이 무너졌다** — 0.571 → 0.353, FN 0 → 3.
회복했던 5건 중 **3건을 다시 잃었다**(DEV-21 · 042 · 053).

**원인: 내가 시키지 않은 단서를 덧붙였다.**
scope 격리만 하면 됐는데 v17 문구에
`"다만 **참가자격으로 요구한 경우**에 한한다. 문서 어딘가에 그 단어가 있다는 것만으로는 아니다"`
를 함께 넣었다. 이게 정확히 그 3건을 죽였다:
```
[상실] DEV-21  "다. 중소기업ㆍ소상공인 확인 대상입니다"      ← 참가자격 절이 아님(견적서 제출·계약방법 절)
[상실] DEV-042 "12) 중·소기업, 소상공인확인서 중 1부"        ← 제출서류 목록
[상실] DEV-053 "②「중소기업기본법」…중소기업 또는 …소상공인"  ← 참가자격 절인데도 상실(변동성 가능)
[유지] DEV-035 · DEV-073                                    ← 둘 다 명시적 입찰참가자격 절
```
5건 중 2건은 **참가자격 절 밖**에 근거가 있었다. 실무에서 기업규모 제한은
'확인 대상', '확인서 제출'처럼 **간접적으로 표현된다** — 내가 넣은 단서는
그 현실을 모르고 만든 억제였다. 억제를 넣지 말라는 원칙을 스스로 어겼다.

**부수 피해**: v13 0.400 → 0.300, v14 0.737 → 0.636(FP 4→7).
v14 는 밴드C 프롬프트에 홀로 남으면서 내가 덧붙인 "이 구간은 기업규모 제한 자체가 불가하다"가
과잉 발화를 부른 것으로 보인다. v10 은 0.444 → 0.480 으로 좋아졌다.

**교훈**: scope 격리(구조)와 문구 수정(내용)을 **한 번에 바꿔서 원인 분리가 안 됐다.**
다음엔 구조만 바꾸고 문구는 글자 그대로 옮겨야 한다.

### 🔬 g2.json ↔ g_k.json 사후 분해 (API 없음) — **이 API 는 결정론적이다**

가장 중요한 발견부터. 이번 변경에서 **G1·G3·G4 프롬프트는 바이트 단위로 동일**했다
(`항목지시` 가 비면 `지침()` 이 기존 `지시` 를 그대로 반환하고, 그 문자열들은 손대지 않았다).
그 2,332칸의 등급 변동:
```
프롬프트 동일 항목(v1~v9 · v19~v24): 변경 0 / 2,332칸 = 0.000%
```
**실행 변동성이 0 이다.** 같은 시드·같은 프롬프트면 등급이 완전히 재현된다.
→ G2 에서 바뀐 24칸은 **전부 프롬프트 변경의 인과 결과**다. 노이즈 해석은 배제된다.
→ 앞으로 프롬프트 A/B 는 **1회 실행으로 인과를 확정할 수 있다.** 반복 측정이 필요 없다.

**등급 변경 전체**: 3,487칸 중 변경 24(0.7%) · 0→2/3 8건 · 2/3→0 15건 · 2↔3 1건 · 결손차이 0

**구조 효과와 문구 효과의 분리** — v10·v11·v13·v18 은 태그(`[경쟁제품 입찰]` 등)와
전문(preamble)만 바뀌고 **새 판정기준이 없다** → 이들의 변화가 순수 구조 효과다.
```
구조만(v10·v11·v13·v18)   ΔTP −2  ΔFP −3
구조+신규문구(v14~v17)      ΔTP −2  ΔFP ±0
```
**구조 격리 자체도 공짜가 아니었다** — FP 를 3건 줄이면서 TP 2건을 잃었다
(v13 DEV-076 · v18 DEV-040). 전문에서 "경쟁제품 입찰인 경우 / 일반 물품·용역인 경우"
절 머리말을 없앤 것이 v13·v18 판단의 맥락을 흐린 것으로 보인다.

**v17 5건 추적 — 단서 조항이 원인임이 확인됐다**
```
공고          등급 i/j/k  최종 i/j/k  k결과  근거 위치
DEV-21        0/3/0       0/1/0       FN    견적서제출·계약방법 절  ← 참가자격 절 아님
DEV-042       0/3/0       0/1/0       FN    제출서류 목록          ← 참가자격 절 아님
DEV-053       0/3/0       0/1/0       FN    입찰참가자격 절
DEV-035       0/3/3       0/1/1       TP    입찰참가자격 절
DEV-073       0/3/3       0/1/1       TP    입찰참가자격 절
```
DEV-21·042 는 근거가 **참가자격 절 밖**이고 정확히 그 둘이 죽었다 —
내가 넣은 "참가자격으로 요구한 경우에 한한다"가 원인이다. 변동성 0 이므로 인과가 확정된다.
**DEV-053 은 참가자격 절인데도 죽었다 → 이 한 건은 원인 확정 불가**
(전문 변경 또는 "※ v17 에만 적용" 문구의 부작용 가능성. 단서 조항만으로는 설명되지 않는다).

**v15/v16 회복 — 문구 효과가 더 직접적인 증거를 갖는다**
```
v15 DEV-067 등급 3→0 · 정답0 → 해소
v15 DEV-076 등급 3→0 · 정답0 → 해소
v16 DEV-067 등급 0→3 · 정답1 → 해소
```
회복 3칸 전부가 **v15·v16 에 새로 넣은 문구**("허용 범위는 중소기업자까지 / 중소기업이
포함된 제한은 적법이며 v15 가 아니다")가 직접 겨냥한 판단이다.
누수 차단(H1)만으로 회복됐다면 v17 지시가 사라진 것이 효과의 전부여야 하는데,
DEV-067 은 v15 를 0 으로 내리면서 **동시에 v16 을 1 로 올렸다** — 이는 "제한을 했다면
중소기업자까지인 한 적법"이라는 v16 신규 문구가 있어야 나오는 연쇄다.
→ **H2(문구 효과)가 더 직접적**이다. 다만 두 변경이 같은 프롬프트에 함께 들어갔으므로
  H1 의 기여를 0 이라 단정할 수는 없다. 분리하려면 문구만 되돌린 1회 실행이 필요하다.

### ✅ dev200l — **새 최고점 0.6758** (2026-09-08)

dev200k 에서 확정된 원인 두 개만 원복했다. 구조(scope 격리)는 그대로 유지.
- v17: "참가자격으로 요구한 경우에 한한다…" **제거** → 절 이름이 아니라 **기능**으로 판단하게 바꿈
  ("어느 절에 적혀 있는지는 따지지 마라 / 참가하려는 자를 구속하는 요건인가")
- v14: "이 구간은 기업규모 제한 자체가 불가하다" **제거**
- v15·v16 신규 문구, v13 충돌 방지, `Group.항목지시` 구조는 유지

| | TP | FP | FN | 정밀도 | 재현율 | Macro F1 |
|---|---|---|---|---|---|---|
| dev200i 원본 | 123 | 142 | 30 | 0.464 | 0.804 | 0.6480 |
| dev200j (직전 best) | 129 | 143 | 24 | 0.474 | 0.843 | 0.6674 |
| dev200k scope+과잉문구 | 125 | 140 | 28 | 0.472 | 0.817 | 0.6585 |
| **dev200l scope+원복** | 128 | **135** | 25 | **0.487** | 0.837 | **0.6758** |

`l − j = +0.0084` · `l − k = +0.0174`. **FP 를 143 → 135 로 8건 줄이면서** 달성했다.

**k → l 변화**: 새 FP 4 · 사라진 FP 9 · **새 FN 0** · 회복 FN 3(v17 2 · v13 1).
회귀 없이 순이득만 냈다.

| 항목 | i | j | k | **l** | l−j |
|---|---|---|---|---|---|
| v14 | 0.778 | 0.737 | 0.636 | **0.778** | +0.041 |
| v15 | 0.923 | 0.800 | 0.923 | **0.923** | +0.123 |
| v16 | 0.526 | 0.444 | 0.526 | **0.526** | +0.082 |
| v17 | 0.182 | **0.571** | 0.353 | 0.500 | −0.071 |
| v13 | 0.348 | 0.400 | 0.300 | 0.400 | ±0 |
| v11 | 0.500 | 0.571 | 0.571 | **0.600** | +0.029 |
| v10 | 0.400 | 0.444 | 0.480 | 0.480 | +0.036 |
| v18 | 0.333 | 0.333 | 0.296 | 0.296 | −0.037 |

**v15·v16 회귀는 재발하지 않았다** — 둘 다 dev200i 수준(FP 1 / FP 8)을 정확히 유지했다.
scope 격리 + v15·v16 문구가 그 회복을 안정적으로 만든다는 것이 두 실행으로 확인됐다.

**v17 은 5건 중 4건 회복** — 기능 기준이 절 밖 근거 2건을 되살렸다.
```
DEV-21  (견적서제출·계약방법 절)  j=1 k=0 → l=1 ✓  등급 3/0/3
DEV-042 (제출서류 목록)          j=1 k=0 → l=1 ✓  등급 3/0/3
DEV-053 (입찰참가자격 절)        j=1 k=0 → l=0 ✗  등급 3/0/0
DEV-035 · DEV-073               계속 유지 ✓
```
**DEV-053 만 여전히 상실**(v17 FN 1). 근거가 참가자격 절 안에 있는데도 죽었으므로
단서 제거로 해결되지 않았다 — 남은 원인은 **전문(preamble) 변경** 또는
"※ v17 에만 적용" 문구뿐이다. 이 한 건 때문에 v17 은 j 대비 −0.071 이지만
v14·v15·v16 이득이 이를 상회한다.

**v18 −0.037 은 미해결**(FP 16, dev200i 대비 FN +1). v13 과 함께 전문 변경의 잔여 영향으로 보인다.

### 📦 09-08 제출본 (submit.zip · 2026-09-08 16:26)

**등급 모드 · 문턱 2 · dev200l 설정 그대로.** dev Macro 0.6758.

⚠️ **제출 직전에 잡은 함정** — 측정은 네 번 다 `--graded` 로 했는데
제출본은 평가 서버가 인자 없이 실행하므로 **이진 모드**로 돌 뻔했다.
두 모드는 프롬프트(등급 안내문)·출력 키·스키마 enum 이 다르고,
**현재 프롬프트로 이진 모드를 측정한 적이 한 번도 없다.**
그대로 냈으면 09-07(미검증 설정 제출 → −0.105)과 같은 실수였다.
→ `build_submit.py` 의 `--graded` 기본을 **True** 로 바꿔 측정과 제출을 일치시켰다.
→ `tools/evaluate.py` 기본도 같이 True 로 맞췄다. **두 기본값은 항상 같아야 한다.**
   이진 모드가 필요하면 양쪽 다 `--binary`.

**문턱 3 을 쓰지 않은 이유**: `g_l.json` 스윕은 문턱 3 이 0.6864(+0.0106)로 더 높다.
하지만 그것은 등급 2 인 19칸을 버리는 **억제**이고, dev 에서 좋아진 억제가
리더보드에서 무너진 것이 정확히 09-07 이다. +0.0106 은 그 위험을 살 만큼 크지 않다.

```
검증  import/문법 OK · test_threshold 7종 PASS · 게이팅 감사 죽인 양성 0
      smoke 자가검증 PASS · 49열 · "판정모드 등급(0~3) · 문턱 2" 확인
      zip 금지어 5종 0건 · script.py 266,174 bytes · submit.zip 97,025 bytes
```

### ✅ dev200m — G1·G4 누수 제거 → **0.6816** (2026-09-08)

G2 에서 검증된 지시문 누수 제거를 G1·G4 에 확장했다. 측정된 누수 실태:
```
G1  126건(63%) 이 v5 를 묻지 않는데 v5 지시 수신 · 29건은 불필요한 지시 5개
    (v5=기준금액 이상 / v6·v7=기준금액 미만 → G2 밴드와 같은 상호배타)
G4  115건(57.5%) 이 **v24 만** 묻는데 설명회 규칙(v22·v23) 둘을 수신
G3  누수 없음 (전 항목 항상 질문)
→ 묻지 않는 항목 1,313개에 지시가 실리고 있었다. 이번에 0 으로 만들었다.
```
**dev200k 의 실수를 반복하지 않았다** — 문구를 글자 그대로 옮기고 전문(preamble)은 손대지 않았다.
공백 정규화 후 원문과 기계 대조해 **완전 일치(내용 변경 0)** 를 확인한 뒤 실행했다.

| | TP | FP | FN | 정밀도 | 재현율 | Macro F1 |
|---|---|---|---|---|---|---|
| dev200l (G2만) | 128 | 135 | 25 | 0.487 | 0.837 | 0.6758 |
| **dev200m (G1·G4 추가)** | 128 | **129** | 25 | **0.498** | 0.837 | **0.6816** |

**TP·FN 은 그대로고 FP 만 6건 줄었다.** 잡음 제거이므로 재현율을 전혀 건드리지 않았다.

```
그룹별 순효과   G1 ΣΔF1 +0.116 · G4 +0.023 · G2 ±0.000 · G3 ±0.000
항목별          v8 +0.083(FP−2) · v3 +0.066 · v4 +0.035 · v24 +0.023(FP−3) · v2 −0.068
```
**G2·G3 가 정확히 0.000 인 것이 결정론을 또 한 번 확인해 준다** — 이번엔 G1·G4 만 바꿨다.

**예상 밖의 결과**: 표적으로 삼았던 v5·v6·v7(상호배타 누수)은 **전혀 움직이지 않았다.**
대신 v8·v3·v4 가 올랐다. v8 은 "실적제한 + 지역제한 둘 다"를 보는 항목이라
불필요한 지역제한 규칙이 사라진 것이 도움이 된 것으로 보이지만 **인과는 확정하지 못했다.**
v2 −0.068(TP −1)은 유일한 회귀다.

**누적 성과** (전부 억제 추가 0)
```
dev200i 0.6480 → j 0.6674 → l 0.6758 → m 0.6816   (+0.0336)
리더보드  0.42458(09-06) → 0.47452(09-08, dev 0.6758 기준)
dev200m 기대치 = 0.6816 × 0.702 ≈ 0.479
```

### ⛔ v24 규칙 배선 A/B — **API 없이 확정. 배선하지 않는다** (2026-09-08)

**API 가 필요 없었다.** `check_v24_positive(rec)` 는 레코드만 보는 결정론 함수이고
`_rule_hints` 는 **LLM 이후 `finalize` 에서** 적용된다 → 배선해도 프롬프트가 안 바뀌므로
등급이 동일하다(결정론은 2,332칸으로 이미 증명). dev200m 등급으로 `finalize` 를 실제
호출해 **정확한 A/B** 를 계산했다. A 재현값이 0.6816 으로 로그와 일치해 방법이 검증됐다.

| | 전체 TP/FP/FN | Macro | v24 TP/FP/FN | v24 F1 |
|---|---|---|---|---|
| **A 현재(dev200m)** | 128/129/25 | **0.6816** | 4/19/4 | 0.258 |
| B1 양성전용 힌트 | 128/131/25 | 0.6810 ↓ | 4/21/4 | 0.242 ↓ |
| B2 규칙단독(0/1 강제) | 126/**112**/27 | **0.6848** ↑ | 2/**2**/6 | **0.333** ↑ |

두 변형 모두 **v24 외 항목은 완전히 동일**했다(설계대로 v24 단독 실험).

**B1 은 기각** — 예전 측정(F1 0.160)과 같은 이유다. 규칙이 맞추는 건을 LLM 이 이미 맞추므로
새 TP 없이 FP 만 늘어난다(19→21).

**B2 는 dev 에서 +0.0032 지만 배선하지 않는다.** 그 +0.0032 의 정체가 문제다:
```
v24 예측 19건 → 2건 (90% 삭감) · 재현율 0.500 → 0.250 · TP 4 → 2
```
**FP 를 17건 줄이는 대신 TP 2건을 버리는 억제**다. 그리고 이 프로젝트에는
"억제는 dev 보다 실제에서 더 크게 깎인다"는 **리더보드 3점 증거**가 있다
(대응비 0.671 → 0.495 → 0.702). dev +0.0032 × 0.702 ≈ **+0.002** 기대이익에
그 위험을 사는 것은 남는 거래가 아니다.

게다가 B2 의 F1 0.333 은 **TP 2건**에 얹혀 있다(양성 8건 표본). 09-07 세부품명 게이팅이
양성 19건에서 dev 상승 → 리더보드 −0.105 였다. 표본은 그때보다 **더 작다.**

**핵심 질문의 답**: "이미 측정된 v24 규칙을 파이프라인에 연결하면 LLM 단독보다 좋아지는가?"
→ **dev 에서는 +0.0032 좋아지지만, 그 개선이 재현율 절반을 팔아 얻은 것이라 채택하지 않는다.**
규칙 F1 0.333 > LLM 0.258 은 사실이나, Macro F1 에서 그 우위는 0.003 에 불과하다.

**dev200m(0.6816)을 baseline 으로 보존한다. 코드 변경 없음.**

### 🎯 전이 격차의 정체 = **정밀도 격차** (2026-09-08 · 중요)

`dev × 0.702` 는 **틀린 모델이었다.** 리더보드에서 역산하면:
```
dev  정밀도 0.498 · 재현율 0.837 · F1 0.6816
실제 F1 0.47452 · 재현율 0.837 가정 → **실제 정밀도 ≈ 0.331** (dev 의 0.66배)
```
실제 세트는 항목당 양성이 dev(3.2%)보다 드물어 같은 FP 가 정밀도를 더 크게 깎는다.
→ **FP 감소는 실제 세트에서 증폭 보상된다.**

| FP 감소 | dev F1 | 실제 F1(추정) |
|---|---|---|
| 0% (현재) | 0.6245 | 0.4745 |
| 25% | 0.6778 | 0.5391 |
| 50% | 0.7411 | 0.6241 |
| **75%** | 0.8174 | **0.7408** |

**LB 0.75 는 불가능하지 않다 — 재현율 0.837 을 지키며 FP 를 75~80% 줄이면 도달한다.**
반대로 재현율을 파는 억제로는 절대 못 간다(정밀도·재현율이 함께 있어야 F1 이 오른다).

### 오류 구성 — **FP 가 84%**
```
오류 154 = FP 129 (84%) + FN 25 (16%)
예측 양성 확신도:  TP {등급3:126, 등급2:2}   FP {등급3:**114**, 등급2:15}
FP 구성:  부재탐지 46 · 비부재·근거있음 76 · 비부재·근거없음 7
```
**FP 129건 중 114건이 등급 3(명백)이다 — 모델이 확신하며 틀린다.**
문턱·확신도 조작으로는 못 고친다(문턱 3 이 +0.0106 에 그친 이유).

### ⛔ 근거문구–기준 정합 검증 — **기각** (offline A/B, API 0회)

가설: 모델이 문항과 무관한 문구를 인용해 FP 가 난다 → 필수 어휘가 없으면 기각.
문항별 최소 필수 어휘군을 정의하고(부재탐지·v3·v8·v23·v24 는 SKIP) 저장 등급으로 정확 측정.
A 재현 검증 통과(TP 128/FP 129/FN 25/Macro 0.6816 정확히 일치).

| | TP | FP | FN | 정밀도 | 재현율 | Macro |
|---|---|---|---|---|---|---|
| A (dev200m) | 128 | 129 | 25 | 0.498 | 0.837 | 0.6816 |
| B (정합검증) | 126 | 125 | 27 | 0.502 | 0.824 | **0.6814** |

**사전 기준(TP 손실 ≤1 · FP 감소 ≥20) 미달 — FP 4건 줄이고 TP 2건을 잃었다.**

**반증된 것**: FP 는 "무관한 인용" 때문이 아니다. 모델은 **관련 있는 문구를 정확히 인용하고
법적 결론만 틀린다.** 제거된 6건이 그대로 보여준다:
```
⚠️TP 손실 DEV-048 v5 "경북에 소재한 실적이 우수한 업체"     ← '경북'을 지역어휘로 못 잡은 내 정규식 결함
⚠️TP 손실 DEV-062 v4 "국가,정부투자기관에서 시행한"          ← 발주처 특정 그 자체인데 '실적' 단어가 없음
 FP 제거 DEV-137 v4 "유사용역 정의(범위): 국가기관 또는 …"   ← 문항과 무관하지 않다. 판단이 틀린 것
 FP 제거 DEV-152 v4/v19 "원제조사(KULTHORN KIRBY社) 증명서"  ← 관련은 있고 요건 해석이 틀림
```
즉 어휘 정합으로 FP 를 걸러낼 수 없다. **FP 는 어휘 문제가 아니라 법적 판단 문제다.**

**부수 확인**: 부재탐지 FP 46건 중 문구가 전체문서에 실제 존재하는 건 14건(30%)뿐이고,
같은 정규식이 TP 에도 7건 발화한다 → 전체문서 스캔을 사실로 주는 안도 여력이 작다.
`full_context` 절단은 12% 레코드에만 발생 → 절단도 주 원인이 아니다.

**production 코드 변경 없음. dev200m(0.6816) 유지.**

### 🏷️ 실제분포 검증셋 구축 설계 (2026-09-08 · 라벨링 전 단계)

**역할 분담이 이 설계의 핵심이다. 두 세트를 섞지 않는다.**
```
dev200      균형 표본(항목당 양성 5~8) · 라벨 있음   → **재현율/FN** 측정
holdout200  실제 분포 층화표본 · 라벨 만든다        → **정밀도/FP** 측정
```
이유: dev 는 인위적 균형이라 FP 의 실제 비용을 못 재고(실제 정밀도 0.331 vs dev 0.498),
holdout200 은 항목당 진짜양성이 약 4건뿐이라 **항목별 재현율을 못 잰다.**
각자 잘 재는 것만 쓴다.

**분포 차이 (실측)**
| | 밴드 A | 밴드 B | 밴드 C | 게이팅 차단 | 판정대상 |
|---|---|---|---|---|---|
| dev200 | 53.0% | 24.5% | 22.5% | 27.4% | 3,487칸 |
| holdout200 | **71.5%** | 17.0% | 11.5% | 31.2% | 3,301칸 |

**기대 규모 (리더보드 역산값 기준)**
```
판정대상 3,301칸 · 예측률 7.43% → 예측양성 약 245칸
그중 진짜양성 약 81 · 진짜양성 총계 약 97 (항목당 약 4.0건)
예측음성 3,056칸 속 FN 약 16건 = 0.52%   ← 음성 표본으로는 못 잡는다
```

**옵션 비교 (2분/칸 가정)**
| | 라벨 수 | 시간 | 얻는 것 | 한계 |
|---|---|---|---|---|
| A 전체 4,800칸 | 4,800 | 160h | 전부 | 항목당 양성 4건 → 항목별 F1 오차 ±0.15 |
| **B 예측양성 전수** | **245** | **8h** | **정밀도·FP 구성 전수** | 재현율 계산 불가 |
| C B + 음성 500 | 745 | 25h | 정밀도 + FN율 | 음성 500칸에 FN 2.6건 → 무의미 |
| D 레코드 40건 전수 | 680 | 23h | 그 40건의 전체 지표 | 진짜양성 19건 → 항목별 불가 |

**선택: B (예측양성 전수 245칸 · 약 8시간).**
FP 감소가 목표이므로 **정밀도만 정확히 재면 충분**하다. 재현율은 dev200 이 담당한다.
C·D 는 비용이 3배인데 FN 추정치가 통계적으로 무의미하다(FN 율 0.52%).

**⚠️ 선행 조건: holdout200 에 현재 코드의 예측이 없다.**
`out/holdout_pred.csv` 는 4세대 전(2단계 검증 포함)이라 예측양성이 61칸(1.27%)뿐이다 —
현재 코드는 약 245칸(7.4%)을 낸다. **4배 차이라 우선순위 산정에 쓸 수 없다.**
→ 예측양성 전수 라벨링을 시작하려면 **holdout200 에 API 1회(800호출)** 가 필요하다.

**만든 것: `tools/make_labelset.py`** (LLM·API 호출 없음)
칸마다 record_id · item · 항목명 · 계약법 · 업무구분 · 추정가격 · 밴드 · 부재탐지 여부 ·
model_grade · model_pred · evidence · **근거문구 주변 원문(요약 안 함)** ·
판정기준(모델이 받은 것과 동일) · 계산된 사실(fact_block·고시대조·모델명후보·지분율·일정검산) ·
human_label · human_confidence · memo 를 낸다. 레코드 단위로 정렬해 문맥 전환 비용을 줄인다.
**`human_label` 은 항상 빈칸이다 — 모델이 대신 채우지 않는다**(검증: 미리채워진 칸 0개).

**라벨링 후 계산할 수 있는 것**
```
✅ 실제분포 정밀도 (전체·항목별)      ← FP 감소 실험의 직접 지표
✅ FP 구성 분해 (항목·밴드·계약법별)
✅ dev 정밀도(0.498) 대비 실제 격차 검증 — 역산값 0.331 이 맞는지 확인
❌ 재현율·F1 (예측음성을 안 보므로) → dev200 으로 대체
```

**앞으로의 A/B 합격 조건 (두 개를 동시에 만족해야 한다)**
```
① holdout200 정밀도 상승 (FP 감소)
② dev200 재현율 불변 (TP 손실 ≤ 1)
```
이 두 조건이 09-07 실패(억제로 재현율 상실)와 오늘 정합검증 실패(TP 2건 손실)를
둘 다 사전에 걸러낸다.

### ⛔ Claude 자가 라벨링 — **기각. 라벨 신뢰도가 모델과 같다** (2026-09-08)

holdout200 을 Claude 가 재판정해 검증셋을 만들려 했다. **먼저 신뢰도를 검증했고 실패했다.**

방법: dev200m 예측양성 259칸에서 FP 다발 항목 위주로 30칸 층화표본을 뽑아
**정답을 가린 상태로** 독립 판정한 뒤 정답과 대조했다(라벨 파일에 정답 미포함).

```
판정 25칸 · 일치 15 · 불일치 10 → **일치율 60.0%** (판단보류 5칸 제외)
같은 25칸에서 파이프라인 모델(전부 1 예측) 정확도 = **60.0%**
→ Claude 가 모델보다 +0.0%
```

**결론: 이 과제에서 Claude 는 gemma 보다 낫지 않다.** 내 라벨로 검증셋을 만들면
모델과 같은 오류를 담은 자기참조 지표가 된다. holdout200 라벨링을 중단했다.

**불일치 10건의 구조 (우연이 아니다)**
```
미달 4건 = 전부 부재탐지(v10·v11·v20)
  → 경쟁제품/SW '적용성'을 확정할 수 없어 0 으로 판정. 정답은 1.
  → 페스티벌·SW유지보수가 중기간 경쟁제품인지 판단하는 데 고시 대조만으로 부족하다.
과예측 6건 중 4건 = 기업규모(v13·v16·v17)
  → '중소기업 또는 소상공인' 병기 해석이 정답과 갈렸다.
```
즉 내 오류는 모델 오류와 **같은 자리**에 난다 — 경쟁제품 판별과 병기 해석.
이 두 곳이 이 과제의 진짜 난점이고, 더 강한 모델을 붙여도 자동으로 해결되지 않는다.

### ⚠️ 정답 라벨에 잡음이 있다 (부수 발견)

`PPS-DEV-042` 와 `PPS-DEV-043` 은 **같은 기관의 같은 금액(61,264,545원) 쌍둥이 공고**이고
규모 표기가 **글자까지 동일**하다 — 둘 다 "중·소기업, 소상공인확인서 중 1부".
그런데 정답이 정반대다:
```
DEV-042  v17=1 · v18=0 · v8=1
DEV-043  v17=0 · v18=1 · v8=0
```
본문 일치율이 14.8% 라 완전 중복은 아니지만, **같은 문구에 반대 라벨**이 붙어 있다.
→ dev 항목별 F1 의 ±0.15 흔들림 중 일부는 모델 탓이 아니라 라벨 잡음이다.
→ 항목당 양성 5~8건에서 이런 쌍이 하나만 있어도 F1 이 0.1 이상 움직인다.
**dev 기반 미세 최적화의 한계를 보여주는 증거다.**

**남은 것**: holdout200 등급(`out/g_holdout.json`)은 추출해 두었다. 라벨은 없지만
**실제 분포에서의 예측률·게이팅 적용률 비교**에는 쓸 수 있다.
사람이 라벨링하는 경로는 여전히 유효하다(`tools/make_labelset.py` 준비됨).

### 🏗️ 판단 구조 분석 (2026-09-08 · 설계만 · 코드/API 0)

**24항목의 판단 구조를 분해했다.** 8개가 '적용성 + 요구사항' **복합 판단**이고
FP 58건 = 전체 129건의 **45%** 를 낸다.
```
복합 항목        추가로 판단해야 하는 적용조건
v10·v11·v13     중기간 경쟁제품인가        ← 하나가 세 항목을 지배
v12             경쟁제품이 **아닌가**       ← v10·v11·v13 과 상호배타
v20             SW사업인가
v9              조달 물품 자체인가
v19             물품공급 확약서인가
v2              제조·용역인가
```
나머지 16개는 게이팅이 적용조건을 **코드로 확정**한다(금액 밴드·낙찰방법·지역기준).
즉 게이팅이 닿지 않는 적용조건만 LLM 이 떠안고, 거기서 FP 절반이 난다.

**A. 네 단계가 섞이는 지점 = `finalize` 가 아니라 프롬프트 한 번**
```
사실추출  fact_block·gosimatch·spec·joint_share·schedule (코드, 결정론)
적용성    게이팅(코드) + **프롬프트 안 LLM** ← 8항목이 여기
요구탐지  **프롬프트 안 LLM** (문서 4,000자 = 중앙값의 30%)
최종판단  **프롬프트 안 LLM** → finalize 는 게이팅·규칙·근거정합만 결합
```
적용성·요구탐지·최종판단 **세 개가 한 번의 호출 안에서 동시에** 일어난다.
평균 4.36문항을 동시에 판단하므로 실제로는 `3 × 4.36` 개의 판단이 한 창에 있다.

**B. 사실블록이 주는 것과 빠진 것**
| 블록 | 주는 것 | 빠진 것 |
|---|---|---|
| `fact_block` | 밴드·지역기준·실적배수·금액대조·조항호 해석 | — (금액·구간은 잘 덮는다) |
| `gosimatch` | 본문 고시번호(용역 27%만) · 물품 유사도 후보(64%) · 용역 29개 목록 | **용역 73% 는 목록만 받고 스스로 고름** |
| `spec` | 모델명 후보 + 대체문구 원문 | — |
| `joint_share` | 지분율 하한 계산 | — |
| `schedule` | v23 일정 검산 | — |
| **없음** | — | **'요구가 문서 어디에 있는가'** (부재탐지 4항목) |

**C. 진단 — 분기 선택은 이미 일관적이다**
```
경쟁제품 계열(v10·v11·v13)과 v12 를 동시에 1 로 낸 모순 예측: **0건**
정답에서도 모순 0건 → 상호배타가 지켜지고 있다
```
그런데 v10·v11·v13 FP 30건의 정답 분기를 보면:
```
정답 '미상'(4형제 전부 0) 28건 · '일반'(v12=1) 1건 · '경쟁제품' 1건
```
**FP 의 93% 는 "분기를 반대로 골랐다"가 아니라 "정답은 이 공고를 경쟁제품 판단
대상으로 아예 보지 않았다"는 것이다.** 즉 모델이 없는 위반을 만들어내는 것이지,
경쟁제품 판별을 반대로 하는 것이 아니다.
→ **'경쟁제품 식별 정확도'를 올리는 설계로는 이 FP 를 못 줄인다.**
   유사도·중간판단·고시대조 확장이 모두 이 지점에서 무력하다.

**D. 중간 단계 추가 비용**
```
현재   공고당 4회 · 평가셋 7,412회 / 6,300초 → 초당 1.18회 필요
+1회   공고당 5회 · 9,265회(1.25배)      → 초당 1.47회 필요
+2회   공고당 6회 · 11,118회(1.50배)     → 초당 1.76회 필요
```
시간예산이 이미 빡빡하다. `deadline` 이 초과를 막지만 초과분은 **0 으로 제출**되므로
재현율을 직접 깎는다. 중간 단계는 **재현율을 시간으로 사는 거래**다.

**E. '모델이 사실을 정답으로 믿는' 문제**
오늘 두 번 실측했다.
```
spec "대체 허용 문구 있음"     → 그 한 줄이 v9 를 0 으로 밀었다 (수정 후 회복)
gosimatch "(확실한 근거)"      → 코드 존재를 적법으로 읽어 v12 를 막았다 (수정 후 v12 +0.127)
```
방어책은 검증됐다: **사실은 위치·인용으로만 주고 결론 어휘를 쓰지 않는다.**
양방향 대칭으로 쓰면 한쪽으로 기울지 않는다(gosimatch 수정이 v10·v11·v13 FP 를 각 3건 줄였다).

**F/G. 이번 단계 결론 — 구현 대상 없음**
후보 3개를 검토했고 셋 다 측정이 지지하지 않았다.
```
① 경쟁제품 식별 강화(유사도/임베딩/중간호출)
   → FP 93% 가 '정답은 판단대상으로 안 봄'이므로 식별을 고쳐도 안 줄어든다. 기각.
② 부재탐지 요구위치 사실 제공
   → FP 14건 회수 가능하나 같은 문구가 TP 7건에도 존재. 순이득 불확실. 보류.
③ 적용성 전용 중간 호출 분리
   → 시간예산 1.25배. 초과분이 0 으로 제출돼 재현율을 깎는다. 이득 근거는 ①에서 이미 기각. 기각.
```
**dev200m 0.6816 / LB 0.47452 를 그대로 보존한다.**

### 📊 실제분포 예측률 실측 — **v18 이 최대 FP 생산자다** (2026-09-08)

holdout200 에 현재 코드로 등급을 추출했다(`out/g_holdout.json` · 라벨 없음 · API 1회).
라벨이 없어도 **예측률과 게이트 적용률**은 정확히 잴 수 있다.

```
dev200m     예측양성 257 / 판정대상 3,487 = 7.37%
holdout200  예측양성 190 / 판정대상 3,301 = 5.76%   ← 실제분포에서 오히려 **낮다**
```
전체 예측률은 dev 의 0.78배다. 그런데 **항목별로는 정반대인 것이 하나 있다.**

**v18 만 2.10배로 폭발한다** (dev 20 → holdout 42).
원인은 게이트 적용률이다 — v18 은 밴드 A 전용이고 holdout 은 밴드 A 가 71.5%(dev 53.0%)다.
```
v18 게이트 열림   dev 106건 → holdout 143건
v17 게이트 열림   dev 106건 → holdout 143건
v16 게이트 열림   dev  49건 → holdout  37건   (밴드 B 는 줄어든다)
v14 게이트 열림   dev  45건 → holdout  26건   (밴드 C 는 더 줄어든다)
```

**실제분포 FP 예산 추정** = holdout 예측수 × (1 − dev 정밀도)
| 항목 | ho 예측 | dev 정밀도 | 추정 FP |
|---|---|---|---|
| **v18** | 42 | 0.200 | **33.6** |
| v24 | 14 | 0.174 | 11.6 |
| v9 | 15 | 0.235 | 11.5 |
| v13 | 11 | 0.286 | 7.9 |
| v17 | 12 | 0.357 | 7.7 |
| v10 | 11 | 0.333 | 7.3 |
```
추정 FP 합계 114 / 예측 190 → 추정 정밀도 0.402
리더보드 역산값 0.331 과 근접 → 이 추정 방식이 타당하다는 교차검증
상위 5개 항목이 추정 FP 의 64%
```

**의미가 큰 결과다.** dev 에서 v18 은 FP 16건으로 v24(19건) 다음이었지만,
**실제 분포에서는 단독 1위이고 2위의 3배다.** 밴드 A 편중이 v18 을 증폭시킨다.
dev 만 보면 이 사실이 보이지 않는다 — dev 의 밴드 A 비율이 실제보다 18.5%p 낮기 때문이다.

**v18 = "1억 미만인데 소기업·소상공인 제한을 아예 안 했으면 위반"** (부재탐지).
dev 정밀도 0.200 · 재현율 0.571 · FP 16건 중 13건이 '적용오판'(문구가 정말 없음).
즉 **제한이 없는 것은 맞는데 정답은 위반이 아니라고 본** 건이 13건이다.
→ 소액·수의계약처럼 판로지원법 기업규모 제한 의무 자체가 없는 구간이 섞여 있을 가능성.
   `_band("A")` 는 추정가격만 보고 계약방법·수의계약 여부는 보지 않는다.
   **다음 조사 대상은 이것이다** (게이팅 변경이 아니라, 정답이 왜 0 인지 규명이 먼저다).

### 🔍 v18 FP 규명 완료 — **원인은 수의계약이 아니라 「제2조의3 예외」다** (2026-09-08)

가설(`_band("A")` 가 계약방법을 안 본다)은 **부분적으로만 맞았고 원인은 다른 것이었다.**

**v18 FP 16건 분류**
```
E 제2조의3 예외 명시    7건  ← 진짜 원인
A 수의계약(예외 미명시)  4건
F/UNKNOWN 원문만으로 불명 5건
```
수의계약 10건 중 6건이 실은 E 유형이다. 계약방법은 **상관변수**였다.

**8/10 수의계약 FP 공고가 판로지원법 시행령 제2조의3 을 본문에 명시한다:**
```
DEV-187 "본 용역은「판로지원법 시행령」제2조의3 제1항 제3호…"
DEV-189 "「판로지원법 시행령」제2조의3 제5호 및…"
DEV-181 "본 용역은「판로지원법 시행령」제2조의3 제1항 및…"
DEV-29·083·115 도 동일
```
**항목표 비고가 정확히 이것을 말한다: "판로지원 예외 명시한 경우, 제한 없어도 가능"**
(v15·v16·v17·v18 네 항목 모두에 같은 비고가 붙어 있다)

**조문 확인** — 시행령 제2조의3①: "…우선조달계약을 체결하지 **않을 수 있다**",
제2조의3②: "그 사유를 **입찰공고문에 기재**하거나 나라장터에 입력하여야 한다".
→ 예외는 **'제한을 안 하는 것'을 허용**한다.

**따라서 예외는 부재탐지 항목에만 적용된다. 측정이 이 법리를 정확히 지지한다:**
| 항목 | 성격 | FP 중 명시 | TP 중 명시 | FN 중 명시 |
|---|---|---|---|---|
| **v18** | 제한 부재 = 위반 | **7/16** | **0/4** | **0/3** |
| **v16** | 제한 부재 = 위반 | **3/8** | **0/5** | 0/1 |
| v15 | 과잉 제한 = 위반 | 0/1 | **3/6** ← 반대 방향 | — |
| v17 | 과잉 제한 = 위반 | 1/9 | 1/5 | 1/1 |

**v16·v18(부재탐지)에서는 FP 10건을 가리키고 TP 위험이 0건이다.**
**v15·v17(과잉제한)에서는 오히려 TP 쪽에 몰린다** — 제한을 했다면 예외를 쓴 게 아니므로 당연하다.

**⚠️ dev200k 실패의 원인이 이제 설명된다.**
그때 넣은 "법정 예외 — v15·v16·v17·v18 에 **모두** 적용된다" 블록은
**v15·v17 에 잘못 적용한 것**이었다. 그래서 v15 가 −0.123 으로 무너졌다.
적용 범위를 **v16·v18 로 좁히면** 그 회귀 없이 FP 만 겨냥할 수 있다.

**현재 프롬프트에는 이 예외가 전혀 없다** (`'제2조의3'`·`'예외'` 둘 다 미포함).
모델은 20% 공고(40/200)가 명시한 법정 예외를 **모른 채** 판단하고 있다.

### 조건별 위험 (v18)
| 조건 | FP | TP | FN | 제거가능 FP | TP 위험 |
|---|---|---|---|---|---|
| **제2조의3 예외 명시** | 7 | 0 | 0 | **7** | **없음** |
| 수의계약(`is_수의`) | 10 | 0 | 0 | 10 | 없음 |
| `소액수의_지방` | 8 | 3 | 2 | 8 | **5건 ⚠️** |
| 수의 AND 예외 미명시 | 4 | 0 | 0 | 4 | 없음 |

`소액수의_지방` 은 TP 3·FN 2 를 함께 죽인다 → **기존 게이팅에 이 조건을 쓰면 안 된다.**

### 결론: **B — 근거는 있으나 게이팅이 아니라 프롬프트로 간다**
- 조문(제2조의3①②) + **항목표 비고**가 명시적으로 뒷받침한다 → 09-07 세부품명(조문 근거 0)과 다르다
- 다만 표본이 작다(v16+v18 양성 13건에서 0/13) → rule of three 95% 상한 23%
- **게이팅·강제규칙은 하지 않는다.** 항목표가 "예외를 **명시한 경우**"라고 조건을 달았으므로
  명시 여부·해당 사유의 타당성은 **읽고 판단할 문제**다. 정규식 `제2조의3` 매칭으로
  강제하면 인용만 하고 사유가 없는 공고까지 죽인다.
- → **v16·v18 항목지시에만** 예외 기준을 추가하는 것이 맞다. `Group.항목지시` 구조가
  이미 항목별 격리를 보장하므로 v15·v17 로 번지지 않는다(dev200k 실패의 재발 방지).

### 🔧 구현 완료 — v16·v18 법정 예외 (2026-09-08 · **API 미검증**)

`pps/prompts.py` 한 파일. `Group.항목지시` 의 **v16·v18 항목에만** 예외 기준 추가.
게이팅·강제규칙·정규식·사실블록·다른 항목 전부 무변경.

**안전장치 두 개를 문구에 넣었다**
1. 예외 사유 4종을 조문에서 그대로 옮겨 적어, 모델이 **어느 사유인지 확인**하게 했다
2. `⚠️ 조항 번호만 인용된 것으로는 부족하다. 위 중 어느 사유인지 공고문에서 확인할 수
   있어야 한다. 사유를 확인할 수 없으면 예외로 보지 않는다.`
   → "제2조의3" 문자열 존재만으로 0 이 되는 것을 막는다(dev FP 7건 중 일부는
     조항만 인용하고 사유가 없을 수 있다 — 그 판단은 모델에게 남긴다)

**검증 (API 0회)**
```
1 import/문법 OK · test_threshold 7종 PASS · 게이팅 감사 죽인 양성 0
2 저장 등급 재채점        0.6816 → 0.6816 · baseline CSV 와 **0칸 차이**
3 전체 TP 128 / FP 129 / FN 25 · Macro 0.6816  → **baseline 완전 보존**
4 v16 5/8/1 · v18 4/16/3 (변화 없음 — 프롬프트 변경은 재채점에 안 나타난다)
5 v15 6/1/0 · v17 5/9/1 · 나머지 20항목 전부 불변
6 scope 격리  밴드A → v18 만 · 밴드B → v16 만 · 밴드C → 0회
              v10~v15·v17 지시에 '법정 예외' 문자열 0
smoke  자가검증 PASS · 49열 · 프롬프트 중앙 6,668자(이전 6,651) · 최대 10,168자
```

⚠️ **2~5 번은 "판정 레이어를 건드리지 않았다"는 증명이지 효과 측정이 아니다.**
`out/g_m.json` 은 옛 프롬프트로 만든 등급이므로 재채점해도 프롬프트 변경이 반영되지 않는다.
0칸 차이가 나온 것이 그 증거다. **효과는 API 1회로만 측정할 수 있다.**

**기대효과 (dev 기준)**: v16 FP 3건 + v18 FP 7건 = 최대 10건 감소, TP 위험 0건.
Macro 로는 v16 0.526→0.588 · v18 0.296→0.400 수준 → 약 **+0.007**.
**실제 분포 기준**: v18 추정 FP 33.6건 중 예외 유형 44% ≈ 15건 = 총 추정 FP 114건의 13%.

### ✅ dev200n 검증 — **표적은 정확히 맞았다. 판정 B(조건부 성공)**

실행 전 상태: 커밋 `c6f1024` + `pps/prompts.py` 미커밋(지문 `320d42d07670b557`) · 등급모드 · 문턱 2.

| | TP | FP | FN | 정밀도 | 재현율 | Macro |
|---|---|---|---|---|---|---|
| dev200m (baseline) | 128 | 129 | 25 | 0.498 | 0.837 | 0.6816 |
| **dev200n** | 127 | **121** | 26 | **0.512** | 0.830 | **0.6850** |
| 변화 | **−1** | **−8** | +1 | +0.014 | −0.007 | **+0.0034** |

**표적 항목은 예측대로 움직였다 — TP 손실 0.**
```
v16  F1 0.526 → 0.625 (+0.099)  TP 5→5 · FP 8→5 · FN 1→1
v18  F1 0.296 → 0.400 (+0.104)  TP 4→4 · FP 16→9 · FN 3→3
v15  F1 0.923 → 0.923 (±0)      완전 불변 ← 범위 격리 성공
v17  F1 0.500 → 0.571 (+0.071)  TP 5→6 · FN 1→0  (예상 밖 개선)
```

**설계 의도 검증 — 사라진 FP 11건 중 10건이 제2조의3 명시 건이다.**
```
DEV-29·083·113·115·181·187·189 (v18) · DEV-32·089·111 (v16)  모두 명시 O
DEV-127 (v18) 만 명시 없음
```
**메커니즘이 의도한 그대로 작동했다.** 우연한 개선이 아니다.

**⚠️ 부작용: 경쟁제품 계열이 흔들렸다 (설계 범위 밖)**
```
v10  F1 0.480 → 0.417 (−0.063)  TP 6→5 · FN 1→2   ← TP 손실
v13  F1 0.400 → 0.300 (−0.100)  TP 4→3 · FN 2→3   ← TP 손실
v11  F1 0.600 → 0.571 (−0.029)  FP 8→9
새 FN 2건: DEV-068 v10 (등급 3→0) · DEV-074 v13 (등급 3→0)
```
v10·v11·v13 지시는 **한 글자도 바꾸지 않았다.** 그런데 같은 G2 프롬프트에
v16·v18 예외 문구가 길게 붙으면서(중앙 6,651→6,668자, 최대 9,318→10,168자)
**같은 창 안의 다른 항목 판단이 흔들렸다.**
```
등급 분포  dev200m {0:3228, 2:18, 3:241} → dev200n {0:3237, 2:19, 3:231}
결손 항목  18 → 24
```
등급 3 이 10칸 줄었다 — 예외 문구가 **G2 전체를 보수적으로** 만들었다.
`Group.항목지시` 가 지시문 **노출**은 격리하지만, 같은 프롬프트를 공유하는 이상
**문맥 간섭까지 막지는 못한다.** 이번에 처음 관측된 한계다.

**순효과**: FP −8 · TP −1 · Macro +0.0034.
정밀도가 0.498 → 0.512 로 올랐고, 실제 분포에서는 정밀도 개선이 증폭되므로
(리더보드 역산 정밀도 0.331) dev 이득보다 클 가능성이 있다.
다만 **TP 1건 손실은 사전 성공조건(TP 손실 0)을 어겼다** → 판정 A 가 아니라 **B**.

### 판정: **A** — 구조는 유지, v17 단서만 원복
근거: 구조 격리는 v15·v16 회복(+0.205)을 만들었고 그 자체의 순효과는 ΔFP −3 / ΔTP −2 로
거의 중립이다. 손실 −0.0090 의 대부분은 **v17 −0.218 한 항목**이고, 그중 2건은
내가 추가한 단서가 원인임이 확정됐다. 구조를 되돌리면 v15·v16 회복까지 잃는다.
⚠️ 단, v14 −0.100(FP +3)은 별도 원인이다 — 밴드C 에 홀로 남은 v14 에
"이 구간은 기업규모 제한 자체가 불가하다"를 덧붙인 것이 과잉 발화를 부른 것으로 보인다.
이것도 신규 문장이므로 같이 원복 대상이다.

**baseline 은 dev200j 로 유지한다.** 되돌리지 않고 원인만 기록한다.

**scope 분리 검증 (API 없음)**
```
밴드A → v17·v18 기준만 실림 (v14·v15·v16 없음)
밴드B → v15·v16 기준만 실림 (v14·v17·v18 없음)   ← v17 병기 기준 완전 제거
밴드C → v14 기준만 실림
v10·v11·v12·v13 은 세 밴드 모두 유지
프롬프트 길이 중앙 6,651자 (dev200j 와 동일) · 최대 9,318자
```

### 판정: **A. 유지**
사전에 못박은 기준(v17 FN 2건 이상 회복 · v13 FP 증가 3건 이하 · Macro 비하락)을
모두 충족했다. v13 FP 는 오히려 3건 줄었다.

### ⚠️ (아래는 검증 전 기록 — 위 결과로 대체됨)
전부 프롬프트·사실블록 문구 변경이라 **API 재실행 없이는 효과를 알 수 없다.**
`out/g.json` 은 **옛 프롬프트**로 만든 등급이므로 재채점해도 프롬프트 변경을 검증하지 못한다
(위에서 0.6480 이 그대로 나온 것이 그 증거다).
→ 제출 전에 dev200 을 등급/이진 모드로 한 번 더 돌려야 판단할 수 있다.

## 실행 방법

```bash
export PPS_API_BASE="https://integrate.api.nvidia.com/v1" PPS_API_MODEL="google/gemma-4-31b-it" PPS_API_RPM=40
export PPS_API_KEY=$(grep -o 'nvapi-[A-Za-z0-9_-]*' apiapiapi.py | head -1)
python3 -u tools/evaluate.py --runner api --save out/x_pred.csv > out/x_eval.log 2>&1
python3 tools/analyze_errors.py out/x_pred.csv
python3 build_submit.py --smoke
```

- `--verify`(2단계 검증)는 **쓰지 말 것** — dev −0.095. 억제 방향이다.
- `tools/calibrate.py` 의 상한은 **의사결정에 쓰지 말 것** (§오프라인 지표 두 개가 모두 실패했다).
- API 응답은 `.cache/api` 에 캐시된다. 같은 프롬프트면 재실행이 0.1초다 —
  진단 스크립트를 짤 때는 `pipeline.render()` 와 **인자를 정확히 같게** 맞춰야 캐시가 맞는다
  (`gosi`·`law_text`·`budget` 누락하면 캐시 미스로 실제 API 를 때린다).
- `| tail` 로 파이프하면 출력이 버퍼링돼 진행이 안 보인다. 리다이렉트 후 tail 할 것.
- 노트북 뚜껑을 덮으면 절전으로 측정이 멈춘다(`caffeinate` 로도 못 막는다).
- API 가 불안정하다 — dev200h 에서 URLError 50여 건으로 빈 출력 58건이 생겼다.
  로그에 `! API 실패` 가 많으면 그 측정은 버려야 한다.
